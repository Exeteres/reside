# Agent Handbook

Project rules live in scoped skills under `.opencode/skills`.

Always start by reading `README.md`, then load `reside-core` for any work in this repository.

Load additional skills as directed by `reside-core` and by the task scope.

Test edit: AGENTS.md write access confirmed again.
