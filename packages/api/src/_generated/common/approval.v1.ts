// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.5
//   protoc               v3.19.1
// source: common/approval.v1.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import type { CallContext, CallOptions } from "nice-grpc-common";
import { Operation } from "./operation.v1";

export const protobufPackage = "reside.common.approval.v1";

/** The result of an approval request, indicating whether it was approved or rejected. */
export enum ApprovalResult {
  /** ESCALATED - The approval request was escalated to the next approval by priority. */
  ESCALATED = 0,
  /** APPROVED - The approval request was approved. */
  APPROVED = 1,
  /** REJECTED - The approval request was rejected. */
  REJECTED = 2,
  UNRECOGNIZED = -1,
}

export function approvalResultFromJSON(object: any): ApprovalResult {
  switch (object) {
    case 0:
    case "ESCALATED":
      return ApprovalResult.ESCALATED;
    case 1:
    case "APPROVED":
      return ApprovalResult.APPROVED;
    case 2:
    case "REJECTED":
      return ApprovalResult.REJECTED;
    case -1:
    case "UNRECOGNIZED":
    default:
      return ApprovalResult.UNRECOGNIZED;
  }
}

export function approvalResultToJSON(object: ApprovalResult): string {
  switch (object) {
    case ApprovalResult.ESCALATED:
      return "ESCALATED";
    case ApprovalResult.APPROVED:
      return "APPROVED";
    case ApprovalResult.REJECTED:
      return "REJECTED";
    case ApprovalResult.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/** The request to approve or reject something in ReSide. */
export interface ApprovalRequest {
  /** The title of the approval request, describing what is being approved. */
  title: string;
  /** The content of the approval request, providing more details about what is being approved. */
  content: string;
}

/** The result of an approval request, indicating whether it was approved or rejected. */
export interface ApprovalResponse {
  /** The result of the approval request. */
  result: ApprovalResult;
  /** The required resultion note provided by the approver describing the reason for result. */
  resolution: string;
}

function createBaseApprovalRequest(): ApprovalRequest {
  return { title: "", content: "" };
}

export const ApprovalRequest: MessageFns<ApprovalRequest> = {
  encode(message: ApprovalRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.title !== "") {
      writer.uint32(10).string(message.title);
    }
    if (message.content !== "") {
      writer.uint32(18).string(message.content);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ApprovalRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseApprovalRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.title = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.content = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ApprovalRequest {
    return {
      title: isSet(object.title) ? globalThis.String(object.title) : "",
      content: isSet(object.content) ? globalThis.String(object.content) : "",
    };
  },

  toJSON(message: ApprovalRequest): unknown {
    const obj: any = {};
    if (message.title !== "") {
      obj.title = message.title;
    }
    if (message.content !== "") {
      obj.content = message.content;
    }
    return obj;
  },

  create(base?: DeepPartial<ApprovalRequest>): ApprovalRequest {
    return ApprovalRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ApprovalRequest>): ApprovalRequest {
    const message = createBaseApprovalRequest();
    message.title = object.title ?? "";
    message.content = object.content ?? "";
    return message;
  },
};

function createBaseApprovalResponse(): ApprovalResponse {
  return { result: 0, resolution: "" };
}

export const ApprovalResponse: MessageFns<ApprovalResponse> = {
  encode(message: ApprovalResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.result !== 0) {
      writer.uint32(8).int32(message.result);
    }
    if (message.resolution !== "") {
      writer.uint32(18).string(message.resolution);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ApprovalResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseApprovalResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.result = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.resolution = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ApprovalResponse {
    return {
      result: isSet(object.result) ? approvalResultFromJSON(object.result) : 0,
      resolution: isSet(object.resolution) ? globalThis.String(object.resolution) : "",
    };
  },

  toJSON(message: ApprovalResponse): unknown {
    const obj: any = {};
    if (message.result !== 0) {
      obj.result = approvalResultToJSON(message.result);
    }
    if (message.resolution !== "") {
      obj.resolution = message.resolution;
    }
    return obj;
  },

  create(base?: DeepPartial<ApprovalResponse>): ApprovalResponse {
    return ApprovalResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ApprovalResponse>): ApprovalResponse {
    const message = createBaseApprovalResponse();
    message.result = object.result ?? 0;
    message.resolution = object.resolution ?? "";
    return message;
  },
};

/** The service that must be implemented by the approver of an approval request. */
export type ApprovalServiceDefinition = typeof ApprovalServiceDefinition;
export const ApprovalServiceDefinition = {
  name: "ApprovalService",
  fullName: "reside.common.approval.v1.ApprovalService",
  methods: {
    /** The Approve method is called to approve or reject an approval request. */
    approve: {
      name: "Approve",
      requestType: ApprovalRequest as typeof ApprovalRequest,
      requestStream: false,
      responseType: Operation as typeof Operation,
      responseStream: false,
      options: {},
    },
  },
} as const;

export interface ApprovalServiceImplementation<CallContextExt = {}> {
  /** The Approve method is called to approve or reject an approval request. */
  approve(request: ApprovalRequest, context: CallContext & CallContextExt): Promise<DeepPartial<Operation>>;
}

export interface ApprovalServiceClient<CallOptionsExt = {}> {
  /** The Approve method is called to approve or reject an approval request. */
  approve(request: DeepPartial<ApprovalRequest>, options?: CallOptions & CallOptionsExt): Promise<Operation>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string; value: unknown } ? { $case: T["$case"]; value?: DeepPartial<T["value"]> }
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
