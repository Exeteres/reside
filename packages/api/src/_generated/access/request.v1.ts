// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.5
//   protoc               v3.19.1
// source: access/request.v1.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import type { CallContext, CallOptions } from "nice-grpc-common";
import { Operation } from "../common/operation.v1";
import { StringValue } from "../google/protobuf/wrappers";
import { DateTime } from "../google/type/datetime";
import { Permission } from "./definition.v1";

export const protobufPackage = "reside.access.request.v1";

/** The status of a permission request set. */
export enum PermissionRequestSetStatus {
  /** PENDING - The request set is pending review. */
  PENDING = 0,
  /** APPROVED - The request set has been approved. */
  APPROVED = 1,
  /** DENIED - The request set has been denied. */
  DENIED = 2,
  /** EXPIRED - The request set expired before review. */
  EXPIRED = 3,
  /** SUPERSEDED - The request set was superseded by a newer request set. */
  SUPERSEDED = 4,
  /** CANCELLED - The request set was cancelled. */
  CANCELLED = 5,
  UNRECOGNIZED = -1,
}

export function permissionRequestSetStatusFromJSON(object: any): PermissionRequestSetStatus {
  switch (object) {
    case 0:
    case "PERMISSION_REQUEST_SET_STATUS_PENDING":
      return PermissionRequestSetStatus.PENDING;
    case 1:
    case "PERMISSION_REQUEST_SET_STATUS_APPROVED":
      return PermissionRequestSetStatus.APPROVED;
    case 2:
    case "PERMISSION_REQUEST_SET_STATUS_DENIED":
      return PermissionRequestSetStatus.DENIED;
    case 3:
    case "PERMISSION_REQUEST_SET_STATUS_EXPIRED":
      return PermissionRequestSetStatus.EXPIRED;
    case 4:
    case "PERMISSION_REQUEST_SET_STATUS_SUPERSEDED":
      return PermissionRequestSetStatus.SUPERSEDED;
    case 5:
    case "PERMISSION_REQUEST_SET_STATUS_CANCELLED":
      return PermissionRequestSetStatus.CANCELLED;
    case -1:
    case "UNRECOGNIZED":
    default:
      return PermissionRequestSetStatus.UNRECOGNIZED;
  }
}

export function permissionRequestSetStatusToJSON(object: PermissionRequestSetStatus): string {
  switch (object) {
    case PermissionRequestSetStatus.PENDING:
      return "PERMISSION_REQUEST_SET_STATUS_PENDING";
    case PermissionRequestSetStatus.APPROVED:
      return "PERMISSION_REQUEST_SET_STATUS_APPROVED";
    case PermissionRequestSetStatus.DENIED:
      return "PERMISSION_REQUEST_SET_STATUS_DENIED";
    case PermissionRequestSetStatus.EXPIRED:
      return "PERMISSION_REQUEST_SET_STATUS_EXPIRED";
    case PermissionRequestSetStatus.SUPERSEDED:
      return "PERMISSION_REQUEST_SET_STATUS_SUPERSEDED";
    case PermissionRequestSetStatus.CANCELLED:
      return "PERMISSION_REQUEST_SET_STATUS_CANCELLED";
    case PermissionRequestSetStatus.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/** A requested permission item inside a permission request set. */
export interface PermissionRequestSetItem {
  /** The permission being requested. */
  permission:
    | Permission
    | undefined;
  /**
   * The optional descriptor of the scope on which the permission is being requested.
   * Cannot be provided for non-scoped permissions that do not require a scope.
   */
  scope: string | undefined;
}

/** The PermissionRequestSet message represents a request set for one or more permissions. */
export interface PermissionRequestSet {
  /** The unique identifier of the permission request set. */
  id: number;
  /** The subject being granted by the permission request set (in format "{realm}:{name}"). */
  subjectId: string;
  /**
   * The required permission set name provided by the caller to identify a replaceable active request set.
   * Scoped to the (subject_id, requested_by_subject_id) pair.
   */
  permissionSetName: string;
  /** The requested permission items. */
  items: PermissionRequestSetItem[];
  /** The required reason provided by the requester for why they are requesting this set. */
  reason: string;
  /** The current status of the permission request set. */
  status: PermissionRequestSetStatus;
  /**
   * The subject that requested the permission set.
   * May not match the subject that will be granted the permissions.
   */
  requestedBySubjectId: string;
  /**
   * The subject that resolved the permission request set.
   * This field is only set if the status is APPROVED or DENIED.
   */
  resolvedBySubjectId:
    | string
    | undefined;
  /**
   * The resolution note provided by the reviewer when approving or denying the request set.
   * This field is only set if the status is APPROVED or DENIED.
   */
  resolution:
    | string
    | undefined;
  /** The ID of the permission request set that superseded this request set, if applicable. */
  supersededByRequestSetId: number;
  /** The timestamp when the permission request set was created. */
  createdAt:
    | DateTime
    | undefined;
  /** The timestamp when the permission request set was resolved (approved, denied, expired, cancelled, or superseded). */
  resolvedAt: DateTime | undefined;
}

/** The input item for requesting a permission in a request set. */
export interface PermissionRequestItemInput {
  /** The unique name of the permission being requested. */
  permissionName: string;
  /**
   * The optional descriptor of the scope on which the permission is being requested.
   * Cannot be provided for non-scoped permissions that do not require a scope.
   */
  scope: string | undefined;
}

/** The request to create a permission request set. */
export interface RequestPermissionsRequest {
  /**
   * The optional identifier of the subject to which requested permissions will be granted (in format "{realm}:{name}").
   * If omitted, falls back to requested_by_subject_id from request metadata.
   */
  subjectId:
    | string
    | undefined;
  /** The list of requested permission items. */
  items: PermissionRequestItemInput[];
  /** The required reason provided by the requester for why they are requesting this permission set. */
  reason: string;
  /**
   * The optional permission set name used to find and supersede the currently active request set before creating a new one.
   * Scoped to the (subject_id, requested_by_subject_id) pair.
   * If omitted, defaults to "default".
   */
  permissionSetName: string | undefined;
}

/** The response for creating a permission request set. */
export interface RequestPermissionsResponse {
  /**
   * The operation used to track asynchronous processing.
   * Present only when at least one permission is missing and a request set is created.
   * Omitted when all requested permissions are already granted.
   */
  operation: Operation | undefined;
}

function createBasePermissionRequestSetItem(): PermissionRequestSetItem {
  return { permission: undefined, scope: undefined };
}

export const PermissionRequestSetItem: MessageFns<PermissionRequestSetItem> = {
  encode(message: PermissionRequestSetItem, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.permission !== undefined) {
      Permission.encode(message.permission, writer.uint32(10).fork()).join();
    }
    if (message.scope !== undefined) {
      StringValue.encode({ value: message.scope! }, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PermissionRequestSetItem {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePermissionRequestSetItem();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.permission = Permission.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.scope = StringValue.decode(reader, reader.uint32()).value;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PermissionRequestSetItem {
    return {
      permission: isSet(object.permission) ? Permission.fromJSON(object.permission) : undefined,
      scope: isSet(object.scope) ? String(object.scope) : undefined,
    };
  },

  toJSON(message: PermissionRequestSetItem): unknown {
    const obj: any = {};
    if (message.permission !== undefined) {
      obj.permission = Permission.toJSON(message.permission);
    }
    if (message.scope !== undefined) {
      obj.scope = message.scope;
    }
    return obj;
  },

  create(base?: DeepPartial<PermissionRequestSetItem>): PermissionRequestSetItem {
    return PermissionRequestSetItem.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PermissionRequestSetItem>): PermissionRequestSetItem {
    const message = createBasePermissionRequestSetItem();
    message.permission = (object.permission !== undefined && object.permission !== null)
      ? Permission.fromPartial(object.permission)
      : undefined;
    message.scope = object.scope ?? undefined;
    return message;
  },
};

function createBasePermissionRequestSet(): PermissionRequestSet {
  return {
    id: 0,
    subjectId: "",
    permissionSetName: "",
    items: [],
    reason: "",
    status: 0,
    requestedBySubjectId: "",
    resolvedBySubjectId: undefined,
    resolution: undefined,
    supersededByRequestSetId: 0,
    createdAt: undefined,
    resolvedAt: undefined,
  };
}

export const PermissionRequestSet: MessageFns<PermissionRequestSet> = {
  encode(message: PermissionRequestSet, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== 0) {
      writer.uint32(8).int32(message.id);
    }
    if (message.subjectId !== "") {
      writer.uint32(18).string(message.subjectId);
    }
    if (message.permissionSetName !== "") {
      writer.uint32(26).string(message.permissionSetName);
    }
    for (const v of message.items) {
      PermissionRequestSetItem.encode(v!, writer.uint32(34).fork()).join();
    }
    if (message.reason !== "") {
      writer.uint32(42).string(message.reason);
    }
    if (message.status !== 0) {
      writer.uint32(48).int32(message.status);
    }
    if (message.requestedBySubjectId !== "") {
      writer.uint32(58).string(message.requestedBySubjectId);
    }
    if (message.resolvedBySubjectId !== undefined) {
      StringValue.encode({ value: message.resolvedBySubjectId! }, writer.uint32(66).fork()).join();
    }
    if (message.resolution !== undefined) {
      StringValue.encode({ value: message.resolution! }, writer.uint32(74).fork()).join();
    }
    if (message.supersededByRequestSetId !== 0) {
      writer.uint32(80).int32(message.supersededByRequestSetId);
    }
    if (message.createdAt !== undefined) {
      DateTime.encode(message.createdAt, writer.uint32(90).fork()).join();
    }
    if (message.resolvedAt !== undefined) {
      DateTime.encode(message.resolvedAt, writer.uint32(98).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PermissionRequestSet {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePermissionRequestSet();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.id = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.subjectId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.permissionSetName = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.items.push(PermissionRequestSetItem.decode(reader, reader.uint32()));
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.reason = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.requestedBySubjectId = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.resolvedBySubjectId = StringValue.decode(reader, reader.uint32()).value;
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.resolution = StringValue.decode(reader, reader.uint32()).value;
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }

          message.supersededByRequestSetId = reader.int32();
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.createdAt = DateTime.decode(reader, reader.uint32());
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.resolvedAt = DateTime.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PermissionRequestSet {
    return {
      id: isSet(object.id) ? globalThis.Number(object.id) : 0,
      subjectId: isSet(object.subjectId)
        ? globalThis.String(object.subjectId)
        : isSet(object.subject_id)
        ? globalThis.String(object.subject_id)
        : "",
      permissionSetName: isSet(object.permissionSetName)
        ? globalThis.String(object.permissionSetName)
        : isSet(object.permission_set_name)
        ? globalThis.String(object.permission_set_name)
        : "",
      items: globalThis.Array.isArray(object?.items)
        ? object.items.map((e: any) => PermissionRequestSetItem.fromJSON(e))
        : [],
      reason: isSet(object.reason) ? globalThis.String(object.reason) : "",
      status: isSet(object.status) ? permissionRequestSetStatusFromJSON(object.status) : 0,
      requestedBySubjectId: isSet(object.requestedBySubjectId)
        ? globalThis.String(object.requestedBySubjectId)
        : isSet(object.requested_by_subject_id)
        ? globalThis.String(object.requested_by_subject_id)
        : "",
      resolvedBySubjectId: isSet(object.resolvedBySubjectId)
        ? String(object.resolvedBySubjectId)
        : isSet(object.resolved_by_subject_id)
        ? String(object.resolved_by_subject_id)
        : undefined,
      resolution: isSet(object.resolution) ? String(object.resolution) : undefined,
      supersededByRequestSetId: isSet(object.supersededByRequestSetId)
        ? globalThis.Number(object.supersededByRequestSetId)
        : isSet(object.superseded_by_request_set_id)
        ? globalThis.Number(object.superseded_by_request_set_id)
        : 0,
      createdAt: isSet(object.createdAt)
        ? DateTime.fromJSON(object.createdAt)
        : isSet(object.created_at)
        ? DateTime.fromJSON(object.created_at)
        : undefined,
      resolvedAt: isSet(object.resolvedAt)
        ? DateTime.fromJSON(object.resolvedAt)
        : isSet(object.resolved_at)
        ? DateTime.fromJSON(object.resolved_at)
        : undefined,
    };
  },

  toJSON(message: PermissionRequestSet): unknown {
    const obj: any = {};
    if (message.id !== 0) {
      obj.id = Math.round(message.id);
    }
    if (message.subjectId !== "") {
      obj.subjectId = message.subjectId;
    }
    if (message.permissionSetName !== "") {
      obj.permissionSetName = message.permissionSetName;
    }
    if (message.items?.length) {
      obj.items = message.items.map((e) => PermissionRequestSetItem.toJSON(e));
    }
    if (message.reason !== "") {
      obj.reason = message.reason;
    }
    if (message.status !== 0) {
      obj.status = permissionRequestSetStatusToJSON(message.status);
    }
    if (message.requestedBySubjectId !== "") {
      obj.requestedBySubjectId = message.requestedBySubjectId;
    }
    if (message.resolvedBySubjectId !== undefined) {
      obj.resolvedBySubjectId = message.resolvedBySubjectId;
    }
    if (message.resolution !== undefined) {
      obj.resolution = message.resolution;
    }
    if (message.supersededByRequestSetId !== 0) {
      obj.supersededByRequestSetId = Math.round(message.supersededByRequestSetId);
    }
    if (message.createdAt !== undefined) {
      obj.createdAt = DateTime.toJSON(message.createdAt);
    }
    if (message.resolvedAt !== undefined) {
      obj.resolvedAt = DateTime.toJSON(message.resolvedAt);
    }
    return obj;
  },

  create(base?: DeepPartial<PermissionRequestSet>): PermissionRequestSet {
    return PermissionRequestSet.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PermissionRequestSet>): PermissionRequestSet {
    const message = createBasePermissionRequestSet();
    message.id = object.id ?? 0;
    message.subjectId = object.subjectId ?? "";
    message.permissionSetName = object.permissionSetName ?? "";
    message.items = object.items?.map((e) => PermissionRequestSetItem.fromPartial(e)) || [];
    message.reason = object.reason ?? "";
    message.status = object.status ?? 0;
    message.requestedBySubjectId = object.requestedBySubjectId ?? "";
    message.resolvedBySubjectId = object.resolvedBySubjectId ?? undefined;
    message.resolution = object.resolution ?? undefined;
    message.supersededByRequestSetId = object.supersededByRequestSetId ?? 0;
    message.createdAt = (object.createdAt !== undefined && object.createdAt !== null)
      ? DateTime.fromPartial(object.createdAt)
      : undefined;
    message.resolvedAt = (object.resolvedAt !== undefined && object.resolvedAt !== null)
      ? DateTime.fromPartial(object.resolvedAt)
      : undefined;
    return message;
  },
};

function createBasePermissionRequestItemInput(): PermissionRequestItemInput {
  return { permissionName: "", scope: undefined };
}

export const PermissionRequestItemInput: MessageFns<PermissionRequestItemInput> = {
  encode(message: PermissionRequestItemInput, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.permissionName !== "") {
      writer.uint32(10).string(message.permissionName);
    }
    if (message.scope !== undefined) {
      StringValue.encode({ value: message.scope! }, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PermissionRequestItemInput {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePermissionRequestItemInput();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.permissionName = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.scope = StringValue.decode(reader, reader.uint32()).value;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PermissionRequestItemInput {
    return {
      permissionName: isSet(object.permissionName)
        ? globalThis.String(object.permissionName)
        : isSet(object.permission_name)
        ? globalThis.String(object.permission_name)
        : "",
      scope: isSet(object.scope) ? String(object.scope) : undefined,
    };
  },

  toJSON(message: PermissionRequestItemInput): unknown {
    const obj: any = {};
    if (message.permissionName !== "") {
      obj.permissionName = message.permissionName;
    }
    if (message.scope !== undefined) {
      obj.scope = message.scope;
    }
    return obj;
  },

  create(base?: DeepPartial<PermissionRequestItemInput>): PermissionRequestItemInput {
    return PermissionRequestItemInput.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PermissionRequestItemInput>): PermissionRequestItemInput {
    const message = createBasePermissionRequestItemInput();
    message.permissionName = object.permissionName ?? "";
    message.scope = object.scope ?? undefined;
    return message;
  },
};

function createBaseRequestPermissionsRequest(): RequestPermissionsRequest {
  return { subjectId: undefined, items: [], reason: "", permissionSetName: undefined };
}

export const RequestPermissionsRequest: MessageFns<RequestPermissionsRequest> = {
  encode(message: RequestPermissionsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.subjectId !== undefined) {
      StringValue.encode({ value: message.subjectId! }, writer.uint32(10).fork()).join();
    }
    for (const v of message.items) {
      PermissionRequestItemInput.encode(v!, writer.uint32(18).fork()).join();
    }
    if (message.reason !== "") {
      writer.uint32(26).string(message.reason);
    }
    if (message.permissionSetName !== undefined) {
      StringValue.encode({ value: message.permissionSetName! }, writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RequestPermissionsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRequestPermissionsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.subjectId = StringValue.decode(reader, reader.uint32()).value;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.items.push(PermissionRequestItemInput.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.reason = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.permissionSetName = StringValue.decode(reader, reader.uint32()).value;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RequestPermissionsRequest {
    return {
      subjectId: isSet(object.subjectId)
        ? String(object.subjectId)
        : isSet(object.subject_id)
        ? String(object.subject_id)
        : undefined,
      items: globalThis.Array.isArray(object?.items)
        ? object.items.map((e: any) => PermissionRequestItemInput.fromJSON(e))
        : [],
      reason: isSet(object.reason) ? globalThis.String(object.reason) : "",
      permissionSetName: isSet(object.permissionSetName)
        ? String(object.permissionSetName)
        : isSet(object.permission_set_name)
        ? String(object.permission_set_name)
        : undefined,
    };
  },

  toJSON(message: RequestPermissionsRequest): unknown {
    const obj: any = {};
    if (message.subjectId !== undefined) {
      obj.subjectId = message.subjectId;
    }
    if (message.items?.length) {
      obj.items = message.items.map((e) => PermissionRequestItemInput.toJSON(e));
    }
    if (message.reason !== "") {
      obj.reason = message.reason;
    }
    if (message.permissionSetName !== undefined) {
      obj.permissionSetName = message.permissionSetName;
    }
    return obj;
  },

  create(base?: DeepPartial<RequestPermissionsRequest>): RequestPermissionsRequest {
    return RequestPermissionsRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<RequestPermissionsRequest>): RequestPermissionsRequest {
    const message = createBaseRequestPermissionsRequest();
    message.subjectId = object.subjectId ?? undefined;
    message.items = object.items?.map((e) => PermissionRequestItemInput.fromPartial(e)) || [];
    message.reason = object.reason ?? "";
    message.permissionSetName = object.permissionSetName ?? undefined;
    return message;
  },
};

function createBaseRequestPermissionsResponse(): RequestPermissionsResponse {
  return { operation: undefined };
}

export const RequestPermissionsResponse: MessageFns<RequestPermissionsResponse> = {
  encode(message: RequestPermissionsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.operation !== undefined) {
      Operation.encode(message.operation, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RequestPermissionsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRequestPermissionsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.operation = Operation.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RequestPermissionsResponse {
    return { operation: isSet(object.operation) ? Operation.fromJSON(object.operation) : undefined };
  },

  toJSON(message: RequestPermissionsResponse): unknown {
    const obj: any = {};
    if (message.operation !== undefined) {
      obj.operation = Operation.toJSON(message.operation);
    }
    return obj;
  },

  create(base?: DeepPartial<RequestPermissionsResponse>): RequestPermissionsResponse {
    return RequestPermissionsResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<RequestPermissionsResponse>): RequestPermissionsResponse {
    const message = createBaseRequestPermissionsResponse();
    message.operation = (object.operation !== undefined && object.operation !== null)
      ? Operation.fromPartial(object.operation)
      : undefined;
    return message;
  },
};

/** The service for handling permission request sets, allowing subjects to request multiple permissions at once. */
export type PermissionRequestServiceDefinition = typeof PermissionRequestServiceDefinition;
export const PermissionRequestServiceDefinition = {
  name: "PermissionRequestService",
  fullName: "reside.access.request.v1.PermissionRequestService",
  methods: {
    /**
     * Submits a new permission request set for the specified subject and permission items.
     * Returns an operation for asynchronous tracking only if there are missing permissions to add.
     * If all requested permissions are already granted, returns an empty response without operation.
     * Returns PERMISSION_DENIED if at least one requested permission is explicitly restricted.
     */
    requestPermissions: {
      name: "RequestPermissions",
      requestType: RequestPermissionsRequest as typeof RequestPermissionsRequest,
      requestStream: false,
      responseType: RequestPermissionsResponse as typeof RequestPermissionsResponse,
      responseStream: false,
      options: {},
    },
  },
} as const;

export interface PermissionRequestServiceImplementation<CallContextExt = {}> {
  /**
   * Submits a new permission request set for the specified subject and permission items.
   * Returns an operation for asynchronous tracking only if there are missing permissions to add.
   * If all requested permissions are already granted, returns an empty response without operation.
   * Returns PERMISSION_DENIED if at least one requested permission is explicitly restricted.
   */
  requestPermissions(
    request: RequestPermissionsRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<RequestPermissionsResponse>>;
}

export interface PermissionRequestServiceClient<CallOptionsExt = {}> {
  /**
   * Submits a new permission request set for the specified subject and permission items.
   * Returns an operation for asynchronous tracking only if there are missing permissions to add.
   * If all requested permissions are already granted, returns an empty response without operation.
   * Returns PERMISSION_DENIED if at least one requested permission is explicitly restricted.
   */
  requestPermissions(
    request: DeepPartial<RequestPermissionsRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<RequestPermissionsResponse>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string; value: unknown } ? { $case: T["$case"]; value?: DeepPartial<T["value"]> }
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
