// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.5
//   protoc               v3.19.1
// source: access/definition.v1.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import type { CallContext, CallOptions } from "nice-grpc-common";
import { StringValue } from "../google/protobuf/wrappers";

export const protobufPackage = "reside.access.definition.v1";

/** The Permission that can be checked against a subject to determine if the subject has the necessary access rights to perform an action on a scope. */
export interface Permission {
  /** The unique numeric identifier of the permission. */
  id: number;
  /** The unique name of the permission. */
  name: string;
  /** The required human-readable name of the permission. */
  title: string;
  /** The optional description of the permission. */
  description:
    | string
    | undefined;
  /** Whether the permission is scoped and requires a scope descriptor to be checked against and granted on. */
  scoped: boolean;
}

/** The Realm represents a logical grouping of users. */
export interface Realm {
  /** The unique numeric identifier of the realm. */
  id: number;
  /** The unique name of the realm. */
  name: string;
  /** The required human-readable name of the realm. */
  title: string;
  /** The optional description of the realm. */
  description:
    | string
    | undefined;
  /** The optional endpoint of reside.common.subject.v1.SubjectService used to resolve subject display info for this realm. */
  subjectServiceEndpoint: string | undefined;
}

/** The input message to create or update a permission in the system. */
export interface PermissionInput {
  /** The unique name of the permission. */
  name: string;
  /** The required human-readable name of the permission. */
  title: string;
  /** The optional description of the permission. */
  description:
    | string
    | undefined;
  /** Whether the permission is scoped and requires a scope descriptor to be checked against and granted on. */
  scoped: boolean;
}

/** The request message to create or update multiple permissions in the system in a batch operation. */
export interface PutPermissionsRequest {
  /**
   * A list of permissions to create or update in the system.
   * Each permission in the list should have a unique name.
   */
  permissions: PermissionInput[];
}

/** The response message for PutPermissionsRequest, containing the list of created or updated permissions. */
export interface PutPermissionsResponse {
  /** A list of permissions that were created or updated as a result of the PutPermissionsRequest. */
  permissions: Permission[];
}

/** The request message to create or update a realm in the system. */
export interface PutRealmRequest {
  /** The name of the realm, which is unique across the system. */
  name: string;
  /** The required human-readable name of the realm. */
  title: string;
  /** The optional description of the realm. */
  description:
    | string
    | undefined;
  /** The optional endpoint of reside.common.subject.v1.SubjectService used to resolve subject display info for this realm. */
  subjectServiceEndpoint: string | undefined;
}

/** The Approver that can approve or deny permission request sets in the system. */
export interface Approver {
  /** The unique numeric identifier of the approver. */
  id: number;
  /** The unique stable name of the approver. */
  name: string;
  /**
   * The approver priority.
   * Approvers with lower priority numbers are evaluated before approvers with higher priority numbers.
   */
  priority: number;
  /**
   * The list of realms this approver can review.
   * If request subject realm is not in this list, the approver is skipped.
   */
  realms: Realm[];
  /** The required human-readable name of the approver. */
  title: string;
  /** The optional description of the approver. */
  description:
    | string
    | undefined;
  /**
   * The callback endpoint to send approve requests.
   * Must implement io.reside.common.approval.v1.ApprovalService.
   */
  callbackEndpoint: string;
}

/** The request to create or update approver in the system. */
export interface PutApproverRequest {
  /** The unique name of the approver. */
  name: string;
  /** The approver priority. */
  priority: number;
  /** The list of realm names this approver can review. */
  realms: string[];
  /** The required human-readable name of the approver. */
  title: string;
  /** The optional description of the approver. */
  description:
    | string
    | undefined;
  /**
   * The callback endpoint to send approve requests.
   * Must implement io.reside.common.approval.v1.ApprovalService.
   */
  callbackEndpoint: string;
}

function createBasePermission(): Permission {
  return { id: 0, name: "", title: "", description: undefined, scoped: false };
}

export const Permission: MessageFns<Permission> = {
  encode(message: Permission, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== 0) {
      writer.uint32(8).int32(message.id);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    if (message.title !== "") {
      writer.uint32(26).string(message.title);
    }
    if (message.description !== undefined) {
      StringValue.encode({ value: message.description! }, writer.uint32(34).fork()).join();
    }
    if (message.scoped !== false) {
      writer.uint32(40).bool(message.scoped);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Permission {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePermission();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.id = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.title = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.description = StringValue.decode(reader, reader.uint32()).value;
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.scoped = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Permission {
    return {
      id: isSet(object.id) ? globalThis.Number(object.id) : 0,
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      title: isSet(object.title) ? globalThis.String(object.title) : "",
      description: isSet(object.description) ? String(object.description) : undefined,
      scoped: isSet(object.scoped) ? globalThis.Boolean(object.scoped) : false,
    };
  },

  toJSON(message: Permission): unknown {
    const obj: any = {};
    if (message.id !== 0) {
      obj.id = Math.round(message.id);
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.title !== "") {
      obj.title = message.title;
    }
    if (message.description !== undefined) {
      obj.description = message.description;
    }
    if (message.scoped !== false) {
      obj.scoped = message.scoped;
    }
    return obj;
  },

  create(base?: DeepPartial<Permission>): Permission {
    return Permission.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Permission>): Permission {
    const message = createBasePermission();
    message.id = object.id ?? 0;
    message.name = object.name ?? "";
    message.title = object.title ?? "";
    message.description = object.description ?? undefined;
    message.scoped = object.scoped ?? false;
    return message;
  },
};

function createBaseRealm(): Realm {
  return { id: 0, name: "", title: "", description: undefined, subjectServiceEndpoint: undefined };
}

export const Realm: MessageFns<Realm> = {
  encode(message: Realm, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== 0) {
      writer.uint32(8).int32(message.id);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    if (message.title !== "") {
      writer.uint32(26).string(message.title);
    }
    if (message.description !== undefined) {
      StringValue.encode({ value: message.description! }, writer.uint32(34).fork()).join();
    }
    if (message.subjectServiceEndpoint !== undefined) {
      StringValue.encode({ value: message.subjectServiceEndpoint! }, writer.uint32(42).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Realm {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRealm();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.id = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.title = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.description = StringValue.decode(reader, reader.uint32()).value;
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.subjectServiceEndpoint = StringValue.decode(reader, reader.uint32()).value;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Realm {
    return {
      id: isSet(object.id) ? globalThis.Number(object.id) : 0,
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      title: isSet(object.title) ? globalThis.String(object.title) : "",
      description: isSet(object.description) ? String(object.description) : undefined,
      subjectServiceEndpoint: isSet(object.subjectServiceEndpoint)
        ? String(object.subjectServiceEndpoint)
        : isSet(object.subject_service_endpoint)
        ? String(object.subject_service_endpoint)
        : undefined,
    };
  },

  toJSON(message: Realm): unknown {
    const obj: any = {};
    if (message.id !== 0) {
      obj.id = Math.round(message.id);
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.title !== "") {
      obj.title = message.title;
    }
    if (message.description !== undefined) {
      obj.description = message.description;
    }
    if (message.subjectServiceEndpoint !== undefined) {
      obj.subjectServiceEndpoint = message.subjectServiceEndpoint;
    }
    return obj;
  },

  create(base?: DeepPartial<Realm>): Realm {
    return Realm.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Realm>): Realm {
    const message = createBaseRealm();
    message.id = object.id ?? 0;
    message.name = object.name ?? "";
    message.title = object.title ?? "";
    message.description = object.description ?? undefined;
    message.subjectServiceEndpoint = object.subjectServiceEndpoint ?? undefined;
    return message;
  },
};

function createBasePermissionInput(): PermissionInput {
  return { name: "", title: "", description: undefined, scoped: false };
}

export const PermissionInput: MessageFns<PermissionInput> = {
  encode(message: PermissionInput, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.title !== "") {
      writer.uint32(18).string(message.title);
    }
    if (message.description !== undefined) {
      StringValue.encode({ value: message.description! }, writer.uint32(26).fork()).join();
    }
    if (message.scoped !== false) {
      writer.uint32(32).bool(message.scoped);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PermissionInput {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePermissionInput();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.title = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.description = StringValue.decode(reader, reader.uint32()).value;
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.scoped = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PermissionInput {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      title: isSet(object.title) ? globalThis.String(object.title) : "",
      description: isSet(object.description) ? String(object.description) : undefined,
      scoped: isSet(object.scoped) ? globalThis.Boolean(object.scoped) : false,
    };
  },

  toJSON(message: PermissionInput): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.title !== "") {
      obj.title = message.title;
    }
    if (message.description !== undefined) {
      obj.description = message.description;
    }
    if (message.scoped !== false) {
      obj.scoped = message.scoped;
    }
    return obj;
  },

  create(base?: DeepPartial<PermissionInput>): PermissionInput {
    return PermissionInput.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PermissionInput>): PermissionInput {
    const message = createBasePermissionInput();
    message.name = object.name ?? "";
    message.title = object.title ?? "";
    message.description = object.description ?? undefined;
    message.scoped = object.scoped ?? false;
    return message;
  },
};

function createBasePutPermissionsRequest(): PutPermissionsRequest {
  return { permissions: [] };
}

export const PutPermissionsRequest: MessageFns<PutPermissionsRequest> = {
  encode(message: PutPermissionsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.permissions) {
      PermissionInput.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PutPermissionsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePutPermissionsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.permissions.push(PermissionInput.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PutPermissionsRequest {
    return {
      permissions: globalThis.Array.isArray(object?.permissions)
        ? object.permissions.map((e: any) => PermissionInput.fromJSON(e))
        : [],
    };
  },

  toJSON(message: PutPermissionsRequest): unknown {
    const obj: any = {};
    if (message.permissions?.length) {
      obj.permissions = message.permissions.map((e) => PermissionInput.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<PutPermissionsRequest>): PutPermissionsRequest {
    return PutPermissionsRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PutPermissionsRequest>): PutPermissionsRequest {
    const message = createBasePutPermissionsRequest();
    message.permissions = object.permissions?.map((e) => PermissionInput.fromPartial(e)) || [];
    return message;
  },
};

function createBasePutPermissionsResponse(): PutPermissionsResponse {
  return { permissions: [] };
}

export const PutPermissionsResponse: MessageFns<PutPermissionsResponse> = {
  encode(message: PutPermissionsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.permissions) {
      Permission.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PutPermissionsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePutPermissionsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.permissions.push(Permission.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PutPermissionsResponse {
    return {
      permissions: globalThis.Array.isArray(object?.permissions)
        ? object.permissions.map((e: any) => Permission.fromJSON(e))
        : [],
    };
  },

  toJSON(message: PutPermissionsResponse): unknown {
    const obj: any = {};
    if (message.permissions?.length) {
      obj.permissions = message.permissions.map((e) => Permission.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<PutPermissionsResponse>): PutPermissionsResponse {
    return PutPermissionsResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PutPermissionsResponse>): PutPermissionsResponse {
    const message = createBasePutPermissionsResponse();
    message.permissions = object.permissions?.map((e) => Permission.fromPartial(e)) || [];
    return message;
  },
};

function createBasePutRealmRequest(): PutRealmRequest {
  return { name: "", title: "", description: undefined, subjectServiceEndpoint: undefined };
}

export const PutRealmRequest: MessageFns<PutRealmRequest> = {
  encode(message: PutRealmRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.title !== "") {
      writer.uint32(18).string(message.title);
    }
    if (message.description !== undefined) {
      StringValue.encode({ value: message.description! }, writer.uint32(26).fork()).join();
    }
    if (message.subjectServiceEndpoint !== undefined) {
      StringValue.encode({ value: message.subjectServiceEndpoint! }, writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PutRealmRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePutRealmRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.title = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.description = StringValue.decode(reader, reader.uint32()).value;
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.subjectServiceEndpoint = StringValue.decode(reader, reader.uint32()).value;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PutRealmRequest {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      title: isSet(object.title) ? globalThis.String(object.title) : "",
      description: isSet(object.description) ? String(object.description) : undefined,
      subjectServiceEndpoint: isSet(object.subjectServiceEndpoint)
        ? String(object.subjectServiceEndpoint)
        : isSet(object.subject_service_endpoint)
        ? String(object.subject_service_endpoint)
        : undefined,
    };
  },

  toJSON(message: PutRealmRequest): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.title !== "") {
      obj.title = message.title;
    }
    if (message.description !== undefined) {
      obj.description = message.description;
    }
    if (message.subjectServiceEndpoint !== undefined) {
      obj.subjectServiceEndpoint = message.subjectServiceEndpoint;
    }
    return obj;
  },

  create(base?: DeepPartial<PutRealmRequest>): PutRealmRequest {
    return PutRealmRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PutRealmRequest>): PutRealmRequest {
    const message = createBasePutRealmRequest();
    message.name = object.name ?? "";
    message.title = object.title ?? "";
    message.description = object.description ?? undefined;
    message.subjectServiceEndpoint = object.subjectServiceEndpoint ?? undefined;
    return message;
  },
};

function createBaseApprover(): Approver {
  return { id: 0, name: "", priority: 0, realms: [], title: "", description: undefined, callbackEndpoint: "" };
}

export const Approver: MessageFns<Approver> = {
  encode(message: Approver, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== 0) {
      writer.uint32(8).int32(message.id);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    if (message.priority !== 0) {
      writer.uint32(24).int32(message.priority);
    }
    for (const v of message.realms) {
      Realm.encode(v!, writer.uint32(34).fork()).join();
    }
    if (message.title !== "") {
      writer.uint32(42).string(message.title);
    }
    if (message.description !== undefined) {
      StringValue.encode({ value: message.description! }, writer.uint32(50).fork()).join();
    }
    if (message.callbackEndpoint !== "") {
      writer.uint32(58).string(message.callbackEndpoint);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Approver {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseApprover();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.id = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.priority = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.realms.push(Realm.decode(reader, reader.uint32()));
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.title = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.description = StringValue.decode(reader, reader.uint32()).value;
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.callbackEndpoint = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Approver {
    return {
      id: isSet(object.id) ? globalThis.Number(object.id) : 0,
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      priority: isSet(object.priority) ? globalThis.Number(object.priority) : 0,
      realms: globalThis.Array.isArray(object?.realms) ? object.realms.map((e: any) => Realm.fromJSON(e)) : [],
      title: isSet(object.title) ? globalThis.String(object.title) : "",
      description: isSet(object.description) ? String(object.description) : undefined,
      callbackEndpoint: isSet(object.callbackEndpoint)
        ? globalThis.String(object.callbackEndpoint)
        : isSet(object.callback_endpoint)
        ? globalThis.String(object.callback_endpoint)
        : "",
    };
  },

  toJSON(message: Approver): unknown {
    const obj: any = {};
    if (message.id !== 0) {
      obj.id = Math.round(message.id);
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.priority !== 0) {
      obj.priority = Math.round(message.priority);
    }
    if (message.realms?.length) {
      obj.realms = message.realms.map((e) => Realm.toJSON(e));
    }
    if (message.title !== "") {
      obj.title = message.title;
    }
    if (message.description !== undefined) {
      obj.description = message.description;
    }
    if (message.callbackEndpoint !== "") {
      obj.callbackEndpoint = message.callbackEndpoint;
    }
    return obj;
  },

  create(base?: DeepPartial<Approver>): Approver {
    return Approver.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Approver>): Approver {
    const message = createBaseApprover();
    message.id = object.id ?? 0;
    message.name = object.name ?? "";
    message.priority = object.priority ?? 0;
    message.realms = object.realms?.map((e) => Realm.fromPartial(e)) || [];
    message.title = object.title ?? "";
    message.description = object.description ?? undefined;
    message.callbackEndpoint = object.callbackEndpoint ?? "";
    return message;
  },
};

function createBasePutApproverRequest(): PutApproverRequest {
  return { name: "", priority: 0, realms: [], title: "", description: undefined, callbackEndpoint: "" };
}

export const PutApproverRequest: MessageFns<PutApproverRequest> = {
  encode(message: PutApproverRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.priority !== 0) {
      writer.uint32(16).int32(message.priority);
    }
    for (const v of message.realms) {
      writer.uint32(26).string(v!);
    }
    if (message.title !== "") {
      writer.uint32(34).string(message.title);
    }
    if (message.description !== undefined) {
      StringValue.encode({ value: message.description! }, writer.uint32(42).fork()).join();
    }
    if (message.callbackEndpoint !== "") {
      writer.uint32(50).string(message.callbackEndpoint);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PutApproverRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePutApproverRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.priority = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.realms.push(reader.string());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.title = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.description = StringValue.decode(reader, reader.uint32()).value;
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.callbackEndpoint = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PutApproverRequest {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      priority: isSet(object.priority) ? globalThis.Number(object.priority) : 0,
      realms: globalThis.Array.isArray(object?.realms) ? object.realms.map((e: any) => globalThis.String(e)) : [],
      title: isSet(object.title) ? globalThis.String(object.title) : "",
      description: isSet(object.description) ? String(object.description) : undefined,
      callbackEndpoint: isSet(object.callbackEndpoint)
        ? globalThis.String(object.callbackEndpoint)
        : isSet(object.callback_endpoint)
        ? globalThis.String(object.callback_endpoint)
        : "",
    };
  },

  toJSON(message: PutApproverRequest): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.priority !== 0) {
      obj.priority = Math.round(message.priority);
    }
    if (message.realms?.length) {
      obj.realms = message.realms;
    }
    if (message.title !== "") {
      obj.title = message.title;
    }
    if (message.description !== undefined) {
      obj.description = message.description;
    }
    if (message.callbackEndpoint !== "") {
      obj.callbackEndpoint = message.callbackEndpoint;
    }
    return obj;
  },

  create(base?: DeepPartial<PutApproverRequest>): PutApproverRequest {
    return PutApproverRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PutApproverRequest>): PutApproverRequest {
    const message = createBasePutApproverRequest();
    message.name = object.name ?? "";
    message.priority = object.priority ?? 0;
    message.realms = object.realms?.map((e) => e) || [];
    message.title = object.title ?? "";
    message.description = object.description ?? undefined;
    message.callbackEndpoint = object.callbackEndpoint ?? "";
    return message;
  },
};

/** The service for managing permissions and roles in the access control system. */
export type DefinitionServiceDefinition = typeof DefinitionServiceDefinition;
export const DefinitionServiceDefinition = {
  name: "DefinitionService",
  fullName: "reside.access.definition.v1.DefinitionService",
  methods: {
    /**
     * Creates or updates permissions in the system.
     * This method is idempotent.
     */
    putPermissions: {
      name: "PutPermissions",
      requestType: PutPermissionsRequest as typeof PutPermissionsRequest,
      requestStream: false,
      responseType: PutPermissionsResponse as typeof PutPermissionsResponse,
      responseStream: false,
      options: {},
    },
    /**
     * Creates or updates a realm in the system.
     * This method is idempotent.
     */
    putRealm: {
      name: "PutRealm",
      requestType: PutRealmRequest as typeof PutRealmRequest,
      requestStream: false,
      responseType: Realm as typeof Realm,
      responseStream: false,
      options: {},
    },
    /**
     * Creates or updates an approver in the system.
     * This method is idempotent.
     */
    putApprover: {
      name: "PutApprover",
      requestType: PutApproverRequest as typeof PutApproverRequest,
      requestStream: false,
      responseType: Approver as typeof Approver,
      responseStream: false,
      options: {},
    },
  },
} as const;

export interface DefinitionServiceImplementation<CallContextExt = {}> {
  /**
   * Creates or updates permissions in the system.
   * This method is idempotent.
   */
  putPermissions(
    request: PutPermissionsRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<PutPermissionsResponse>>;
  /**
   * Creates or updates a realm in the system.
   * This method is idempotent.
   */
  putRealm(request: PutRealmRequest, context: CallContext & CallContextExt): Promise<DeepPartial<Realm>>;
  /**
   * Creates or updates an approver in the system.
   * This method is idempotent.
   */
  putApprover(request: PutApproverRequest, context: CallContext & CallContextExt): Promise<DeepPartial<Approver>>;
}

export interface DefinitionServiceClient<CallOptionsExt = {}> {
  /**
   * Creates or updates permissions in the system.
   * This method is idempotent.
   */
  putPermissions(
    request: DeepPartial<PutPermissionsRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<PutPermissionsResponse>;
  /**
   * Creates or updates a realm in the system.
   * This method is idempotent.
   */
  putRealm(request: DeepPartial<PutRealmRequest>, options?: CallOptions & CallOptionsExt): Promise<Realm>;
  /**
   * Creates or updates an approver in the system.
   * This method is idempotent.
   */
  putApprover(request: DeepPartial<PutApproverRequest>, options?: CallOptions & CallOptionsExt): Promise<Approver>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string; value: unknown } ? { $case: T["$case"]; value?: DeepPartial<T["value"]> }
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
