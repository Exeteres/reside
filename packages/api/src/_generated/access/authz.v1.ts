// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.5
//   protoc               v3.19.1
// source: access/authz.v1.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import type { CallContext, CallOptions } from "nice-grpc-common";
import { StringValue } from "../google/protobuf/wrappers";

export const protobufPackage = "reside.access.authz.v1";

/** The request message to check if a subject has a specific permission on a scope. */
export interface CheckPermissionRequest {
  /** The unique name of the permission to check. */
  permissionName: string;
  /** The unique identifier of the subject (in format "{realm}:{name}") for which the permission check is being performed. */
  subjectId: string;
  /**
   * The scope descriptor for which the permission check is being performed.
   * This field is optional and must be provided if and only if the permission being checked is scoped.
   */
  scope: string | undefined;
}

/** The response message indicating whether the subject has the specified permission. */
export interface CheckPermissionResponse {
  /** The boolean value indicating whether the subject has the specified permission. */
  authorized: boolean;
}

function createBaseCheckPermissionRequest(): CheckPermissionRequest {
  return { permissionName: "", subjectId: "", scope: undefined };
}

export const CheckPermissionRequest: MessageFns<CheckPermissionRequest> = {
  encode(message: CheckPermissionRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.permissionName !== "") {
      writer.uint32(10).string(message.permissionName);
    }
    if (message.subjectId !== "") {
      writer.uint32(18).string(message.subjectId);
    }
    if (message.scope !== undefined) {
      StringValue.encode({ value: message.scope! }, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CheckPermissionRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCheckPermissionRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.permissionName = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.subjectId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.scope = StringValue.decode(reader, reader.uint32()).value;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CheckPermissionRequest {
    return {
      permissionName: isSet(object.permissionName)
        ? globalThis.String(object.permissionName)
        : isSet(object.permission_name)
        ? globalThis.String(object.permission_name)
        : "",
      subjectId: isSet(object.subjectId)
        ? globalThis.String(object.subjectId)
        : isSet(object.subject_id)
        ? globalThis.String(object.subject_id)
        : "",
      scope: isSet(object.scope) ? String(object.scope) : undefined,
    };
  },

  toJSON(message: CheckPermissionRequest): unknown {
    const obj: any = {};
    if (message.permissionName !== "") {
      obj.permissionName = message.permissionName;
    }
    if (message.subjectId !== "") {
      obj.subjectId = message.subjectId;
    }
    if (message.scope !== undefined) {
      obj.scope = message.scope;
    }
    return obj;
  },

  create(base?: DeepPartial<CheckPermissionRequest>): CheckPermissionRequest {
    return CheckPermissionRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<CheckPermissionRequest>): CheckPermissionRequest {
    const message = createBaseCheckPermissionRequest();
    message.permissionName = object.permissionName ?? "";
    message.subjectId = object.subjectId ?? "";
    message.scope = object.scope ?? undefined;
    return message;
  },
};

function createBaseCheckPermissionResponse(): CheckPermissionResponse {
  return { authorized: false };
}

export const CheckPermissionResponse: MessageFns<CheckPermissionResponse> = {
  encode(message: CheckPermissionResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.authorized !== false) {
      writer.uint32(8).bool(message.authorized);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CheckPermissionResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCheckPermissionResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.authorized = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CheckPermissionResponse {
    return { authorized: isSet(object.authorized) ? globalThis.Boolean(object.authorized) : false };
  },

  toJSON(message: CheckPermissionResponse): unknown {
    const obj: any = {};
    if (message.authorized !== false) {
      obj.authorized = message.authorized;
    }
    return obj;
  },

  create(base?: DeepPartial<CheckPermissionResponse>): CheckPermissionResponse {
    return CheckPermissionResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<CheckPermissionResponse>): CheckPermissionResponse {
    const message = createBaseCheckPermissionResponse();
    message.authorized = object.authorized ?? false;
    return message;
  },
};

/** The AuthzService defines the RPC method for checking permissions of a subject. */
export type AuthzServiceDefinition = typeof AuthzServiceDefinition;
export const AuthzServiceDefinition = {
  name: "AuthzService",
  fullName: "reside.access.authz.v1.AuthzService",
  methods: {
    /** Checks if the specified subject has the given permission on the scope. */
    checkPermission: {
      name: "CheckPermission",
      requestType: CheckPermissionRequest as typeof CheckPermissionRequest,
      requestStream: false,
      responseType: CheckPermissionResponse as typeof CheckPermissionResponse,
      responseStream: false,
      options: {},
    },
  },
} as const;

export interface AuthzServiceImplementation<CallContextExt = {}> {
  /** Checks if the specified subject has the given permission on the scope. */
  checkPermission(
    request: CheckPermissionRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<CheckPermissionResponse>>;
}

export interface AuthzServiceClient<CallOptionsExt = {}> {
  /** Checks if the specified subject has the given permission on the scope. */
  checkPermission(
    request: DeepPartial<CheckPermissionRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<CheckPermissionResponse>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string; value: unknown } ? { $case: T["$case"]; value?: DeepPartial<T["value"]> }
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
