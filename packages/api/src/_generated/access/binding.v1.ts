// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.5
//   protoc               v3.19.1
// source: access/binding.v1.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import type { CallContext, CallOptions } from "nice-grpc-common";
import { Timestamp } from "../google/protobuf/timestamp";
import { StringValue } from "../google/protobuf/wrappers";

export const protobufPackage = "reside.access.binding.v1";

/**
 * The PermissionBinding message represents the association between a permission and a subject (e.g., user, service account) that has been granted that permission.
 * The binding is identified by the combination of permission_id, subject_id, and an optional scope descriptor.
 */
export interface PermissionBinding {
  /** The unique identifier of the permission. */
  permissionId: number;
  /** The unique identifier of the subject (in format "{realm}:{name}") that is bound to the permission. */
  subjectId: string;
  /**
   * The optional scope descriptor for which the permission is granted.
   * If not provided, the permission is granted globally.
   */
  scope:
    | string
    | undefined;
  /**
   * The timestamp when the permission binding was last created.
   * If a binding is removed and then re-created, the created_at timestamp will reflect the most recent creation time of the binding.
   */
  createdAt: Date | undefined;
}

/**
 * The explicit restriction of some subject from having a permission.
 * Used to prevent requesting permissions that we previously denied in other requests.
 */
export interface PermissionRestriction {
  /** The unique identifier of the permission. */
  permissionId: number;
  /** The unique identifier of the subject (in format "{realm}:{name}") that is restricted from the permission. */
  subjectId: string;
  /**
   * The optional scope descriptor for which the permission is restricted.
   * If not provided, the restriction applies globally.
   */
  scope:
    | string
    | undefined;
  /**
   * The timestamp when the permission restriction was created.
   * If a restriction is removed and then re-created, the created_at timestamp will reflect the most recent creation time of the restriction.
   */
  createdAt: Date | undefined;
}

/** The request message to list all permission bindings for a specific subject. */
export interface ListPermissionBindingsRequest {
  /** The unique identifier of the subject (in format "{realm}:{name}") for which to list permission bindings. */
  subjectId: string;
}

/** The response message containing the list of permission bindings. */
export interface ListPermissionBindingsResponse {
  /** The list of permission bindings that match the request filters. */
  bindings: PermissionBinding[];
}

/** The request message to list all permission restrictions for a specific subject. */
export interface ListPermissionRestrictionsRequest {
  /** The unique identifier of the subject (in format "{realm}:{name}") for which to list permission restrictions. */
  subjectId: string;
}

/** The response message containing the list of permission restrictions. */
export interface ListPermissionRestrictionsResponse {
  /** The list of permission restrictions that match the request filters. */
  restrictions: PermissionRestriction[];
}

function createBasePermissionBinding(): PermissionBinding {
  return { permissionId: 0, subjectId: "", scope: undefined, createdAt: undefined };
}

export const PermissionBinding: MessageFns<PermissionBinding> = {
  encode(message: PermissionBinding, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.permissionId !== 0) {
      writer.uint32(8).int32(message.permissionId);
    }
    if (message.subjectId !== "") {
      writer.uint32(18).string(message.subjectId);
    }
    if (message.scope !== undefined) {
      StringValue.encode({ value: message.scope! }, writer.uint32(26).fork()).join();
    }
    if (message.createdAt !== undefined) {
      Timestamp.encode(toTimestamp(message.createdAt), writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PermissionBinding {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePermissionBinding();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.permissionId = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.subjectId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.scope = StringValue.decode(reader, reader.uint32()).value;
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.createdAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PermissionBinding {
    return {
      permissionId: isSet(object.permissionId)
        ? globalThis.Number(object.permissionId)
        : isSet(object.permission_id)
        ? globalThis.Number(object.permission_id)
        : 0,
      subjectId: isSet(object.subjectId)
        ? globalThis.String(object.subjectId)
        : isSet(object.subject_id)
        ? globalThis.String(object.subject_id)
        : "",
      scope: isSet(object.scope) ? String(object.scope) : undefined,
      createdAt: isSet(object.createdAt)
        ? fromJsonTimestamp(object.createdAt)
        : isSet(object.created_at)
        ? fromJsonTimestamp(object.created_at)
        : undefined,
    };
  },

  toJSON(message: PermissionBinding): unknown {
    const obj: any = {};
    if (message.permissionId !== 0) {
      obj.permissionId = Math.round(message.permissionId);
    }
    if (message.subjectId !== "") {
      obj.subjectId = message.subjectId;
    }
    if (message.scope !== undefined) {
      obj.scope = message.scope;
    }
    if (message.createdAt !== undefined) {
      obj.createdAt = message.createdAt.toISOString();
    }
    return obj;
  },

  create(base?: DeepPartial<PermissionBinding>): PermissionBinding {
    return PermissionBinding.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PermissionBinding>): PermissionBinding {
    const message = createBasePermissionBinding();
    message.permissionId = object.permissionId ?? 0;
    message.subjectId = object.subjectId ?? "";
    message.scope = object.scope ?? undefined;
    message.createdAt = object.createdAt ?? undefined;
    return message;
  },
};

function createBasePermissionRestriction(): PermissionRestriction {
  return { permissionId: 0, subjectId: "", scope: undefined, createdAt: undefined };
}

export const PermissionRestriction: MessageFns<PermissionRestriction> = {
  encode(message: PermissionRestriction, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.permissionId !== 0) {
      writer.uint32(8).int32(message.permissionId);
    }
    if (message.subjectId !== "") {
      writer.uint32(18).string(message.subjectId);
    }
    if (message.scope !== undefined) {
      StringValue.encode({ value: message.scope! }, writer.uint32(26).fork()).join();
    }
    if (message.createdAt !== undefined) {
      Timestamp.encode(toTimestamp(message.createdAt), writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PermissionRestriction {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePermissionRestriction();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.permissionId = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.subjectId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.scope = StringValue.decode(reader, reader.uint32()).value;
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.createdAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PermissionRestriction {
    return {
      permissionId: isSet(object.permissionId)
        ? globalThis.Number(object.permissionId)
        : isSet(object.permission_id)
        ? globalThis.Number(object.permission_id)
        : 0,
      subjectId: isSet(object.subjectId)
        ? globalThis.String(object.subjectId)
        : isSet(object.subject_id)
        ? globalThis.String(object.subject_id)
        : "",
      scope: isSet(object.scope) ? String(object.scope) : undefined,
      createdAt: isSet(object.createdAt)
        ? fromJsonTimestamp(object.createdAt)
        : isSet(object.created_at)
        ? fromJsonTimestamp(object.created_at)
        : undefined,
    };
  },

  toJSON(message: PermissionRestriction): unknown {
    const obj: any = {};
    if (message.permissionId !== 0) {
      obj.permissionId = Math.round(message.permissionId);
    }
    if (message.subjectId !== "") {
      obj.subjectId = message.subjectId;
    }
    if (message.scope !== undefined) {
      obj.scope = message.scope;
    }
    if (message.createdAt !== undefined) {
      obj.createdAt = message.createdAt.toISOString();
    }
    return obj;
  },

  create(base?: DeepPartial<PermissionRestriction>): PermissionRestriction {
    return PermissionRestriction.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PermissionRestriction>): PermissionRestriction {
    const message = createBasePermissionRestriction();
    message.permissionId = object.permissionId ?? 0;
    message.subjectId = object.subjectId ?? "";
    message.scope = object.scope ?? undefined;
    message.createdAt = object.createdAt ?? undefined;
    return message;
  },
};

function createBaseListPermissionBindingsRequest(): ListPermissionBindingsRequest {
  return { subjectId: "" };
}

export const ListPermissionBindingsRequest: MessageFns<ListPermissionBindingsRequest> = {
  encode(message: ListPermissionBindingsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.subjectId !== "") {
      writer.uint32(10).string(message.subjectId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListPermissionBindingsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListPermissionBindingsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.subjectId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListPermissionBindingsRequest {
    return {
      subjectId: isSet(object.subjectId)
        ? globalThis.String(object.subjectId)
        : isSet(object.subject_id)
        ? globalThis.String(object.subject_id)
        : "",
    };
  },

  toJSON(message: ListPermissionBindingsRequest): unknown {
    const obj: any = {};
    if (message.subjectId !== "") {
      obj.subjectId = message.subjectId;
    }
    return obj;
  },

  create(base?: DeepPartial<ListPermissionBindingsRequest>): ListPermissionBindingsRequest {
    return ListPermissionBindingsRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ListPermissionBindingsRequest>): ListPermissionBindingsRequest {
    const message = createBaseListPermissionBindingsRequest();
    message.subjectId = object.subjectId ?? "";
    return message;
  },
};

function createBaseListPermissionBindingsResponse(): ListPermissionBindingsResponse {
  return { bindings: [] };
}

export const ListPermissionBindingsResponse: MessageFns<ListPermissionBindingsResponse> = {
  encode(message: ListPermissionBindingsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.bindings) {
      PermissionBinding.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListPermissionBindingsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListPermissionBindingsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.bindings.push(PermissionBinding.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListPermissionBindingsResponse {
    return {
      bindings: globalThis.Array.isArray(object?.bindings)
        ? object.bindings.map((e: any) => PermissionBinding.fromJSON(e))
        : [],
    };
  },

  toJSON(message: ListPermissionBindingsResponse): unknown {
    const obj: any = {};
    if (message.bindings?.length) {
      obj.bindings = message.bindings.map((e) => PermissionBinding.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<ListPermissionBindingsResponse>): ListPermissionBindingsResponse {
    return ListPermissionBindingsResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ListPermissionBindingsResponse>): ListPermissionBindingsResponse {
    const message = createBaseListPermissionBindingsResponse();
    message.bindings = object.bindings?.map((e) => PermissionBinding.fromPartial(e)) || [];
    return message;
  },
};

function createBaseListPermissionRestrictionsRequest(): ListPermissionRestrictionsRequest {
  return { subjectId: "" };
}

export const ListPermissionRestrictionsRequest: MessageFns<ListPermissionRestrictionsRequest> = {
  encode(message: ListPermissionRestrictionsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.subjectId !== "") {
      writer.uint32(10).string(message.subjectId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListPermissionRestrictionsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListPermissionRestrictionsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.subjectId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListPermissionRestrictionsRequest {
    return {
      subjectId: isSet(object.subjectId)
        ? globalThis.String(object.subjectId)
        : isSet(object.subject_id)
        ? globalThis.String(object.subject_id)
        : "",
    };
  },

  toJSON(message: ListPermissionRestrictionsRequest): unknown {
    const obj: any = {};
    if (message.subjectId !== "") {
      obj.subjectId = message.subjectId;
    }
    return obj;
  },

  create(base?: DeepPartial<ListPermissionRestrictionsRequest>): ListPermissionRestrictionsRequest {
    return ListPermissionRestrictionsRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ListPermissionRestrictionsRequest>): ListPermissionRestrictionsRequest {
    const message = createBaseListPermissionRestrictionsRequest();
    message.subjectId = object.subjectId ?? "";
    return message;
  },
};

function createBaseListPermissionRestrictionsResponse(): ListPermissionRestrictionsResponse {
  return { restrictions: [] };
}

export const ListPermissionRestrictionsResponse: MessageFns<ListPermissionRestrictionsResponse> = {
  encode(message: ListPermissionRestrictionsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.restrictions) {
      PermissionRestriction.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListPermissionRestrictionsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListPermissionRestrictionsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.restrictions.push(PermissionRestriction.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListPermissionRestrictionsResponse {
    return {
      restrictions: globalThis.Array.isArray(object?.restrictions)
        ? object.restrictions.map((e: any) => PermissionRestriction.fromJSON(e))
        : [],
    };
  },

  toJSON(message: ListPermissionRestrictionsResponse): unknown {
    const obj: any = {};
    if (message.restrictions?.length) {
      obj.restrictions = message.restrictions.map((e) => PermissionRestriction.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<ListPermissionRestrictionsResponse>): ListPermissionRestrictionsResponse {
    return ListPermissionRestrictionsResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ListPermissionRestrictionsResponse>): ListPermissionRestrictionsResponse {
    const message = createBaseListPermissionRestrictionsResponse();
    message.restrictions = object.restrictions?.map((e) => PermissionRestriction.fromPartial(e)) || [];
    return message;
  },
};

/** The service for querying permission bindings in the access control system. */
export type BindingServiceDefinition = typeof BindingServiceDefinition;
export const BindingServiceDefinition = {
  name: "BindingService",
  fullName: "reside.access.binding.v1.BindingService",
  methods: {
    /** Lists permission bindings for the given subject. */
    listPermissionBindings: {
      name: "ListPermissionBindings",
      requestType: ListPermissionBindingsRequest as typeof ListPermissionBindingsRequest,
      requestStream: false,
      responseType: ListPermissionBindingsResponse as typeof ListPermissionBindingsResponse,
      responseStream: false,
      options: {},
    },
    /** Lists permission restrictions for the given subject. */
    listPermissionRestrictions: {
      name: "ListPermissionRestrictions",
      requestType: ListPermissionRestrictionsRequest as typeof ListPermissionRestrictionsRequest,
      requestStream: false,
      responseType: ListPermissionRestrictionsResponse as typeof ListPermissionRestrictionsResponse,
      responseStream: false,
      options: {},
    },
  },
} as const;

export interface BindingServiceImplementation<CallContextExt = {}> {
  /** Lists permission bindings for the given subject. */
  listPermissionBindings(
    request: ListPermissionBindingsRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<ListPermissionBindingsResponse>>;
  /** Lists permission restrictions for the given subject. */
  listPermissionRestrictions(
    request: ListPermissionRestrictionsRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<ListPermissionRestrictionsResponse>>;
}

export interface BindingServiceClient<CallOptionsExt = {}> {
  /** Lists permission bindings for the given subject. */
  listPermissionBindings(
    request: DeepPartial<ListPermissionBindingsRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<ListPermissionBindingsResponse>;
  /** Lists permission restrictions for the given subject. */
  listPermissionRestrictions(
    request: DeepPartial<ListPermissionRestrictionsRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<ListPermissionRestrictionsResponse>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string; value: unknown } ? { $case: T["$case"]; value?: DeepPartial<T["value"]> }
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000).toString();
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (globalThis.Number(t.seconds) || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
