// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.5
//   protoc               v3.19.1
// source: database/provision.v1.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import type { CallContext, CallOptions } from "nice-grpc-common";
import { Operation } from "../common/operation.v1";
import { Empty } from "../google/protobuf/empty";

export const protobufPackage = "reside.database.provision.v1";

/** The object describing the credentials for a provisioned PostgreSQL database. */
export interface PostgresDatabaseCredentials {
  host: string;
  port: number;
  database: string;
  username: string;
  password: string;
}

/** The object describing the credentials for a provisioned temporal namespace. */
export interface TemporalNamespaceCredentials {
  address: string;
  namespace: string;
}

/**
 * The response for a GetTemporalNamespaceCredentials request,
 * which may contain either the credentials or an operation if the namespace is not yet ready.
 * The result of the operation, once completed, will be the TemporalNamespaceCredentials.
 * Clients should poll the operation using reside.common.operation.v1.OperationService.
 */
export interface GetTemporalNamespaceCredentialsResponse {
  credentials:
    | { $case: "result"; value: TemporalNamespaceCredentials }
    | { $case: "operation"; value: Operation }
    | undefined;
}

/**
 * The response for a GetPostgresDatabaseCredentialsRequest,
 * which may contain either the credentials or an operation if the credentials are not yet ready.
 * The result of the operation, once completed, will be the PostgresDatabaseCredentials.
 * Clients should poll the operation using reside.common.operation.v1.OperationService.
 */
export interface GetPostgresDatabaseCredentialsResponse {
  credentials:
    | { $case: "result"; value: PostgresDatabaseCredentials }
    | { $case: "operation"; value: Operation }
    | undefined;
}

function createBasePostgresDatabaseCredentials(): PostgresDatabaseCredentials {
  return { host: "", port: 0, database: "", username: "", password: "" };
}

export const PostgresDatabaseCredentials: MessageFns<PostgresDatabaseCredentials> = {
  encode(message: PostgresDatabaseCredentials, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.host !== "") {
      writer.uint32(10).string(message.host);
    }
    if (message.port !== 0) {
      writer.uint32(16).int32(message.port);
    }
    if (message.database !== "") {
      writer.uint32(26).string(message.database);
    }
    if (message.username !== "") {
      writer.uint32(34).string(message.username);
    }
    if (message.password !== "") {
      writer.uint32(42).string(message.password);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PostgresDatabaseCredentials {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePostgresDatabaseCredentials();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.host = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.port = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.database = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.username = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.password = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PostgresDatabaseCredentials {
    return {
      host: isSet(object.host) ? globalThis.String(object.host) : "",
      port: isSet(object.port) ? globalThis.Number(object.port) : 0,
      database: isSet(object.database) ? globalThis.String(object.database) : "",
      username: isSet(object.username) ? globalThis.String(object.username) : "",
      password: isSet(object.password) ? globalThis.String(object.password) : "",
    };
  },

  toJSON(message: PostgresDatabaseCredentials): unknown {
    const obj: any = {};
    if (message.host !== "") {
      obj.host = message.host;
    }
    if (message.port !== 0) {
      obj.port = Math.round(message.port);
    }
    if (message.database !== "") {
      obj.database = message.database;
    }
    if (message.username !== "") {
      obj.username = message.username;
    }
    if (message.password !== "") {
      obj.password = message.password;
    }
    return obj;
  },

  create(base?: DeepPartial<PostgresDatabaseCredentials>): PostgresDatabaseCredentials {
    return PostgresDatabaseCredentials.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PostgresDatabaseCredentials>): PostgresDatabaseCredentials {
    const message = createBasePostgresDatabaseCredentials();
    message.host = object.host ?? "";
    message.port = object.port ?? 0;
    message.database = object.database ?? "";
    message.username = object.username ?? "";
    message.password = object.password ?? "";
    return message;
  },
};

function createBaseTemporalNamespaceCredentials(): TemporalNamespaceCredentials {
  return { address: "", namespace: "" };
}

export const TemporalNamespaceCredentials: MessageFns<TemporalNamespaceCredentials> = {
  encode(message: TemporalNamespaceCredentials, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.address !== "") {
      writer.uint32(10).string(message.address);
    }
    if (message.namespace !== "") {
      writer.uint32(18).string(message.namespace);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TemporalNamespaceCredentials {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTemporalNamespaceCredentials();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.address = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.namespace = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TemporalNamespaceCredentials {
    return {
      address: isSet(object.address) ? globalThis.String(object.address) : "",
      namespace: isSet(object.namespace) ? globalThis.String(object.namespace) : "",
    };
  },

  toJSON(message: TemporalNamespaceCredentials): unknown {
    const obj: any = {};
    if (message.address !== "") {
      obj.address = message.address;
    }
    if (message.namespace !== "") {
      obj.namespace = message.namespace;
    }
    return obj;
  },

  create(base?: DeepPartial<TemporalNamespaceCredentials>): TemporalNamespaceCredentials {
    return TemporalNamespaceCredentials.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<TemporalNamespaceCredentials>): TemporalNamespaceCredentials {
    const message = createBaseTemporalNamespaceCredentials();
    message.address = object.address ?? "";
    message.namespace = object.namespace ?? "";
    return message;
  },
};

function createBaseGetTemporalNamespaceCredentialsResponse(): GetTemporalNamespaceCredentialsResponse {
  return { credentials: undefined };
}

export const GetTemporalNamespaceCredentialsResponse: MessageFns<GetTemporalNamespaceCredentialsResponse> = {
  encode(message: GetTemporalNamespaceCredentialsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    switch (message.credentials?.$case) {
      case "result":
        TemporalNamespaceCredentials.encode(message.credentials.value, writer.uint32(10).fork()).join();
        break;
      case "operation":
        Operation.encode(message.credentials.value, writer.uint32(18).fork()).join();
        break;
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetTemporalNamespaceCredentialsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetTemporalNamespaceCredentialsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.credentials = {
            $case: "result",
            value: TemporalNamespaceCredentials.decode(reader, reader.uint32()),
          };
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.credentials = { $case: "operation", value: Operation.decode(reader, reader.uint32()) };
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetTemporalNamespaceCredentialsResponse {
    return {
      credentials: isSet(object.result)
        ? { $case: "result", value: TemporalNamespaceCredentials.fromJSON(object.result) }
        : isSet(object.operation)
        ? { $case: "operation", value: Operation.fromJSON(object.operation) }
        : undefined,
    };
  },

  toJSON(message: GetTemporalNamespaceCredentialsResponse): unknown {
    const obj: any = {};
    if (message.credentials?.$case === "result") {
      obj.result = TemporalNamespaceCredentials.toJSON(message.credentials.value);
    } else if (message.credentials?.$case === "operation") {
      obj.operation = Operation.toJSON(message.credentials.value);
    }
    return obj;
  },

  create(base?: DeepPartial<GetTemporalNamespaceCredentialsResponse>): GetTemporalNamespaceCredentialsResponse {
    return GetTemporalNamespaceCredentialsResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetTemporalNamespaceCredentialsResponse>): GetTemporalNamespaceCredentialsResponse {
    const message = createBaseGetTemporalNamespaceCredentialsResponse();
    switch (object.credentials?.$case) {
      case "result": {
        if (object.credentials?.value !== undefined && object.credentials?.value !== null) {
          message.credentials = {
            $case: "result",
            value: TemporalNamespaceCredentials.fromPartial(object.credentials.value),
          };
        }
        break;
      }
      case "operation": {
        if (object.credentials?.value !== undefined && object.credentials?.value !== null) {
          message.credentials = { $case: "operation", value: Operation.fromPartial(object.credentials.value) };
        }
        break;
      }
    }
    return message;
  },
};

function createBaseGetPostgresDatabaseCredentialsResponse(): GetPostgresDatabaseCredentialsResponse {
  return { credentials: undefined };
}

export const GetPostgresDatabaseCredentialsResponse: MessageFns<GetPostgresDatabaseCredentialsResponse> = {
  encode(message: GetPostgresDatabaseCredentialsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    switch (message.credentials?.$case) {
      case "result":
        PostgresDatabaseCredentials.encode(message.credentials.value, writer.uint32(10).fork()).join();
        break;
      case "operation":
        Operation.encode(message.credentials.value, writer.uint32(18).fork()).join();
        break;
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetPostgresDatabaseCredentialsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetPostgresDatabaseCredentialsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.credentials = { $case: "result", value: PostgresDatabaseCredentials.decode(reader, reader.uint32()) };
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.credentials = { $case: "operation", value: Operation.decode(reader, reader.uint32()) };
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetPostgresDatabaseCredentialsResponse {
    return {
      credentials: isSet(object.result)
        ? { $case: "result", value: PostgresDatabaseCredentials.fromJSON(object.result) }
        : isSet(object.operation)
        ? { $case: "operation", value: Operation.fromJSON(object.operation) }
        : undefined,
    };
  },

  toJSON(message: GetPostgresDatabaseCredentialsResponse): unknown {
    const obj: any = {};
    if (message.credentials?.$case === "result") {
      obj.result = PostgresDatabaseCredentials.toJSON(message.credentials.value);
    } else if (message.credentials?.$case === "operation") {
      obj.operation = Operation.toJSON(message.credentials.value);
    }
    return obj;
  },

  create(base?: DeepPartial<GetPostgresDatabaseCredentialsResponse>): GetPostgresDatabaseCredentialsResponse {
    return GetPostgresDatabaseCredentialsResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetPostgresDatabaseCredentialsResponse>): GetPostgresDatabaseCredentialsResponse {
    const message = createBaseGetPostgresDatabaseCredentialsResponse();
    switch (object.credentials?.$case) {
      case "result": {
        if (object.credentials?.value !== undefined && object.credentials?.value !== null) {
          message.credentials = {
            $case: "result",
            value: PostgresDatabaseCredentials.fromPartial(object.credentials.value),
          };
        }
        break;
      }
      case "operation": {
        if (object.credentials?.value !== undefined && object.credentials?.value !== null) {
          message.credentials = { $case: "operation", value: Operation.fromPartial(object.credentials.value) };
        }
        break;
      }
    }
    return message;
  },
};

/** The service responsible for provisioning personal PostgreSQL databases for replicas. */
export type ProvisionServiceDefinition = typeof ProvisionServiceDefinition;
export const ProvisionServiceDefinition = {
  name: "ProvisionService",
  fullName: "reside.database.provision.v1.ProvisionService",
  methods: {
    /** Gets credentials for the caller's personal PostgreSQL database. */
    getPostgresDatabaseCredentials: {
      name: "GetPostgresDatabaseCredentials",
      requestType: Empty as typeof Empty,
      requestStream: false,
      responseType: GetPostgresDatabaseCredentialsResponse as typeof GetPostgresDatabaseCredentialsResponse,
      responseStream: false,
      options: {},
    },
    /** Gets credentials for the caller's personal temporal namespace. */
    getTemporalNamespaceCredentials: {
      name: "GetTemporalNamespaceCredentials",
      requestType: Empty as typeof Empty,
      requestStream: false,
      responseType: GetTemporalNamespaceCredentialsResponse as typeof GetTemporalNamespaceCredentialsResponse,
      responseStream: false,
      options: {},
    },
  },
} as const;

export interface ProvisionServiceImplementation<CallContextExt = {}> {
  /** Gets credentials for the caller's personal PostgreSQL database. */
  getPostgresDatabaseCredentials(
    request: Empty,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<GetPostgresDatabaseCredentialsResponse>>;
  /** Gets credentials for the caller's personal temporal namespace. */
  getTemporalNamespaceCredentials(
    request: Empty,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<GetTemporalNamespaceCredentialsResponse>>;
}

export interface ProvisionServiceClient<CallOptionsExt = {}> {
  /** Gets credentials for the caller's personal PostgreSQL database. */
  getPostgresDatabaseCredentials(
    request: DeepPartial<Empty>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<GetPostgresDatabaseCredentialsResponse>;
  /** Gets credentials for the caller's personal temporal namespace. */
  getTemporalNamespaceCredentials(
    request: DeepPartial<Empty>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<GetTemporalNamespaceCredentialsResponse>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string; value: unknown } ? { $case: T["$case"]; value?: DeepPartial<T["value"]> }
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
