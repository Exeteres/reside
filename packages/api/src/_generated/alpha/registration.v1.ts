// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.5
//   protoc               v3.19.1
// source: alpha/registration.v1.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import type { CallContext, CallOptions } from "nice-grpc-common";
import { Operation } from "../common/operation.v1";
import { StringValue } from "../google/protobuf/wrappers";

export const protobufPackage = "reside.alpha.registration.v1";

/** The message describing the dependency slot for the replica dependency. */
export interface ReplicaDependencySlot {
  /** The name of the dependency slot. */
  name: string;
  /** The default name of the replica that occupies this slot. */
  defaultReplicaName: string | undefined;
}

/** The message describing the dependency slot for the replica endpoint. */
export interface ReplicaEndpointDependencySlot {
  /** The name of the dependency slot. */
  name: string;
  /** The default value of the endpoint. */
  defaultEndpoint: string | undefined;
}

/**
 * The request message for registering a new replica in Alpha Replica.
 * Must be auhenticated with Kubernetes SA.
 */
export interface RegisterReplicaRequest {
  /** The required human-readable name of the replica. */
  title: string;
  /** The optional description of the replica. */
  description: string;
  /** The url of the replica avatar if any. */
  avatarUrl:
    | string
    | undefined;
  /** The current internal endpoint of the replica. */
  internalEndpoint: string;
  /** The current public endpoint of the replica if any. */
  publicEndpoint:
    | string
    | undefined;
  /** The list of dependency slots for other replicas that this replica requires to function properly. */
  replicaDependencies: ReplicaDependencySlot[];
  /**
   * The list of dependency slots for endpoints that this replica requires to function properly.
   * If endpoint value is not provided or overriden and matching resolved replica dependency slot is occupied,
   * the endpoint value from the occupied replica will be used.
   */
  endpointDependencies: ReplicaEndpointDependencySlot[];
}

/** The service that must be used by all replicas to register themselves in the cluster. */
export interface RegisterReplicaResponse {
  /** The operation that should be awaited if registration requires reconciliation before becoming ready. */
  operation: Operation | undefined;
}

function createBaseReplicaDependencySlot(): ReplicaDependencySlot {
  return { name: "", defaultReplicaName: undefined };
}

export const ReplicaDependencySlot: MessageFns<ReplicaDependencySlot> = {
  encode(message: ReplicaDependencySlot, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.defaultReplicaName !== undefined) {
      StringValue.encode({ value: message.defaultReplicaName! }, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ReplicaDependencySlot {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseReplicaDependencySlot();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.defaultReplicaName = StringValue.decode(reader, reader.uint32()).value;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ReplicaDependencySlot {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      defaultReplicaName: isSet(object.defaultReplicaName)
        ? String(object.defaultReplicaName)
        : isSet(object.default_replica_name)
        ? String(object.default_replica_name)
        : undefined,
    };
  },

  toJSON(message: ReplicaDependencySlot): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.defaultReplicaName !== undefined) {
      obj.defaultReplicaName = message.defaultReplicaName;
    }
    return obj;
  },

  create(base?: DeepPartial<ReplicaDependencySlot>): ReplicaDependencySlot {
    return ReplicaDependencySlot.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ReplicaDependencySlot>): ReplicaDependencySlot {
    const message = createBaseReplicaDependencySlot();
    message.name = object.name ?? "";
    message.defaultReplicaName = object.defaultReplicaName ?? undefined;
    return message;
  },
};

function createBaseReplicaEndpointDependencySlot(): ReplicaEndpointDependencySlot {
  return { name: "", defaultEndpoint: undefined };
}

export const ReplicaEndpointDependencySlot: MessageFns<ReplicaEndpointDependencySlot> = {
  encode(message: ReplicaEndpointDependencySlot, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.defaultEndpoint !== undefined) {
      StringValue.encode({ value: message.defaultEndpoint! }, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ReplicaEndpointDependencySlot {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseReplicaEndpointDependencySlot();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.defaultEndpoint = StringValue.decode(reader, reader.uint32()).value;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ReplicaEndpointDependencySlot {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      defaultEndpoint: isSet(object.defaultEndpoint)
        ? String(object.defaultEndpoint)
        : isSet(object.default_endpoint)
        ? String(object.default_endpoint)
        : undefined,
    };
  },

  toJSON(message: ReplicaEndpointDependencySlot): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.defaultEndpoint !== undefined) {
      obj.defaultEndpoint = message.defaultEndpoint;
    }
    return obj;
  },

  create(base?: DeepPartial<ReplicaEndpointDependencySlot>): ReplicaEndpointDependencySlot {
    return ReplicaEndpointDependencySlot.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ReplicaEndpointDependencySlot>): ReplicaEndpointDependencySlot {
    const message = createBaseReplicaEndpointDependencySlot();
    message.name = object.name ?? "";
    message.defaultEndpoint = object.defaultEndpoint ?? undefined;
    return message;
  },
};

function createBaseRegisterReplicaRequest(): RegisterReplicaRequest {
  return {
    title: "",
    description: "",
    avatarUrl: undefined,
    internalEndpoint: "",
    publicEndpoint: undefined,
    replicaDependencies: [],
    endpointDependencies: [],
  };
}

export const RegisterReplicaRequest: MessageFns<RegisterReplicaRequest> = {
  encode(message: RegisterReplicaRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.title !== "") {
      writer.uint32(10).string(message.title);
    }
    if (message.description !== "") {
      writer.uint32(18).string(message.description);
    }
    if (message.avatarUrl !== undefined) {
      StringValue.encode({ value: message.avatarUrl! }, writer.uint32(26).fork()).join();
    }
    if (message.internalEndpoint !== "") {
      writer.uint32(34).string(message.internalEndpoint);
    }
    if (message.publicEndpoint !== undefined) {
      StringValue.encode({ value: message.publicEndpoint! }, writer.uint32(42).fork()).join();
    }
    for (const v of message.replicaDependencies) {
      ReplicaDependencySlot.encode(v!, writer.uint32(50).fork()).join();
    }
    for (const v of message.endpointDependencies) {
      ReplicaEndpointDependencySlot.encode(v!, writer.uint32(58).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RegisterReplicaRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRegisterReplicaRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.title = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.description = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.avatarUrl = StringValue.decode(reader, reader.uint32()).value;
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.internalEndpoint = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.publicEndpoint = StringValue.decode(reader, reader.uint32()).value;
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.replicaDependencies.push(ReplicaDependencySlot.decode(reader, reader.uint32()));
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.endpointDependencies.push(ReplicaEndpointDependencySlot.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RegisterReplicaRequest {
    return {
      title: isSet(object.title) ? globalThis.String(object.title) : "",
      description: isSet(object.description) ? globalThis.String(object.description) : "",
      avatarUrl: isSet(object.avatarUrl)
        ? String(object.avatarUrl)
        : isSet(object.avatar_url)
        ? String(object.avatar_url)
        : undefined,
      internalEndpoint: isSet(object.internalEndpoint)
        ? globalThis.String(object.internalEndpoint)
        : isSet(object.internal_endpoint)
        ? globalThis.String(object.internal_endpoint)
        : "",
      publicEndpoint: isSet(object.publicEndpoint)
        ? String(object.publicEndpoint)
        : isSet(object.public_endpoint)
        ? String(object.public_endpoint)
        : undefined,
      replicaDependencies: globalThis.Array.isArray(object?.replicaDependencies)
        ? object.replicaDependencies.map((e: any) => ReplicaDependencySlot.fromJSON(e))
        : globalThis.Array.isArray(object?.replica_dependencies)
        ? object.replica_dependencies.map((e: any) => ReplicaDependencySlot.fromJSON(e))
        : [],
      endpointDependencies: globalThis.Array.isArray(object?.endpointDependencies)
        ? object.endpointDependencies.map((e: any) => ReplicaEndpointDependencySlot.fromJSON(e))
        : globalThis.Array.isArray(object?.endpoint_dependencies)
        ? object.endpoint_dependencies.map((e: any) => ReplicaEndpointDependencySlot.fromJSON(e))
        : [],
    };
  },

  toJSON(message: RegisterReplicaRequest): unknown {
    const obj: any = {};
    if (message.title !== "") {
      obj.title = message.title;
    }
    if (message.description !== "") {
      obj.description = message.description;
    }
    if (message.avatarUrl !== undefined) {
      obj.avatarUrl = message.avatarUrl;
    }
    if (message.internalEndpoint !== "") {
      obj.internalEndpoint = message.internalEndpoint;
    }
    if (message.publicEndpoint !== undefined) {
      obj.publicEndpoint = message.publicEndpoint;
    }
    if (message.replicaDependencies?.length) {
      obj.replicaDependencies = message.replicaDependencies.map((e) => ReplicaDependencySlot.toJSON(e));
    }
    if (message.endpointDependencies?.length) {
      obj.endpointDependencies = message.endpointDependencies.map((e) => ReplicaEndpointDependencySlot.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<RegisterReplicaRequest>): RegisterReplicaRequest {
    return RegisterReplicaRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<RegisterReplicaRequest>): RegisterReplicaRequest {
    const message = createBaseRegisterReplicaRequest();
    message.title = object.title ?? "";
    message.description = object.description ?? "";
    message.avatarUrl = object.avatarUrl ?? undefined;
    message.internalEndpoint = object.internalEndpoint ?? "";
    message.publicEndpoint = object.publicEndpoint ?? undefined;
    message.replicaDependencies = object.replicaDependencies?.map((e) => ReplicaDependencySlot.fromPartial(e)) || [];
    message.endpointDependencies =
      object.endpointDependencies?.map((e) => ReplicaEndpointDependencySlot.fromPartial(e)) || [];
    return message;
  },
};

function createBaseRegisterReplicaResponse(): RegisterReplicaResponse {
  return { operation: undefined };
}

export const RegisterReplicaResponse: MessageFns<RegisterReplicaResponse> = {
  encode(message: RegisterReplicaResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.operation !== undefined) {
      Operation.encode(message.operation, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RegisterReplicaResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRegisterReplicaResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.operation = Operation.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RegisterReplicaResponse {
    return { operation: isSet(object.operation) ? Operation.fromJSON(object.operation) : undefined };
  },

  toJSON(message: RegisterReplicaResponse): unknown {
    const obj: any = {};
    if (message.operation !== undefined) {
      obj.operation = Operation.toJSON(message.operation);
    }
    return obj;
  },

  create(base?: DeepPartial<RegisterReplicaResponse>): RegisterReplicaResponse {
    return RegisterReplicaResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<RegisterReplicaResponse>): RegisterReplicaResponse {
    const message = createBaseRegisterReplicaResponse();
    message.operation = (object.operation !== undefined && object.operation !== null)
      ? Operation.fromPartial(object.operation)
      : undefined;
    return message;
  },
};

/** The service that must be used by all replicas to register themselves in the cluster. */
export type RegistrationServiceDefinition = typeof RegistrationServiceDefinition;
export const RegistrationServiceDefinition = {
  name: "RegistrationService",
  fullName: "reside.alpha.registration.v1.RegistrationService",
  methods: {
    /** RegisterReplica registers a new replica and its identity in Alpha. */
    registerReplica: {
      name: "RegisterReplica",
      requestType: RegisterReplicaRequest as typeof RegisterReplicaRequest,
      requestStream: false,
      responseType: RegisterReplicaResponse as typeof RegisterReplicaResponse,
      responseStream: false,
      options: {},
    },
  },
} as const;

export interface RegistrationServiceImplementation<CallContextExt = {}> {
  /** RegisterReplica registers a new replica and its identity in Alpha. */
  registerReplica(
    request: RegisterReplicaRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<RegisterReplicaResponse>>;
}

export interface RegistrationServiceClient<CallOptionsExt = {}> {
  /** RegisterReplica registers a new replica and its identity in Alpha. */
  registerReplica(
    request: DeepPartial<RegisterReplicaRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<RegisterReplicaResponse>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string; value: unknown } ? { $case: T["$case"]; value?: DeepPartial<T["value"]> }
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
