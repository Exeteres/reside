// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.5
//   protoc               v3.19.1
// source: alpha/load.v1.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import type { CallContext, CallOptions } from "nice-grpc-common";
import { Empty } from "../google/protobuf/empty";

export const protobufPackage = "reside.alpha.load.v1";

/** The request message to load the new replica in the cluster or update the existing one. */
export interface LoadReplicaRequest {
  /** The name of the replica to load or update. */
  name: string;
  /** The image of the replica. */
  image: string;
}

function createBaseLoadReplicaRequest(): LoadReplicaRequest {
  return { name: "", image: "" };
}

export const LoadReplicaRequest: MessageFns<LoadReplicaRequest> = {
  encode(message: LoadReplicaRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.image !== "") {
      writer.uint32(18).string(message.image);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): LoadReplicaRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLoadReplicaRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.image = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): LoadReplicaRequest {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      image: isSet(object.image) ? globalThis.String(object.image) : "",
    };
  },

  toJSON(message: LoadReplicaRequest): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.image !== "") {
      obj.image = message.image;
    }
    return obj;
  },

  create(base?: DeepPartial<LoadReplicaRequest>): LoadReplicaRequest {
    return LoadReplicaRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<LoadReplicaRequest>): LoadReplicaRequest {
    const message = createBaseLoadReplicaRequest();
    message.name = object.name ?? "";
    message.image = object.image ?? "";
    return message;
  },
};

/** The service that must be used to initiate loading of a new replica or update the existing one in the cluster. */
export type LoadServiceDefinition = typeof LoadServiceDefinition;
export const LoadServiceDefinition = {
  name: "LoadService",
  fullName: "reside.alpha.load.v1.LoadService",
  methods: {
    /** Requests the Alpha Replica to load a new replica or update the existing one in the cluster. */
    loadReplica: {
      name: "LoadReplica",
      requestType: LoadReplicaRequest as typeof LoadReplicaRequest,
      requestStream: false,
      responseType: Empty as typeof Empty,
      responseStream: false,
      options: {},
    },
  },
} as const;

export interface LoadServiceImplementation<CallContextExt = {}> {
  /** Requests the Alpha Replica to load a new replica or update the existing one in the cluster. */
  loadReplica(request: LoadReplicaRequest, context: CallContext & CallContextExt): Promise<DeepPartial<Empty>>;
}

export interface LoadServiceClient<CallOptionsExt = {}> {
  /** Requests the Alpha Replica to load a new replica or update the existing one in the cluster. */
  loadReplica(request: DeepPartial<LoadReplicaRequest>, options?: CallOptions & CallOptionsExt): Promise<Empty>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string; value: unknown } ? { $case: T["$case"]; value?: DeepPartial<T["value"]> }
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
