// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.5
//   protoc               v3.19.1
// source: interaction/notification.v1.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import type { CallContext, CallOptions } from "nice-grpc-common";
import { InlineFile } from "../common/file.v1";
import { Operation } from "../common/operation.v1";
import { BoolValue, StringValue } from "../google/protobuf/wrappers";

export const protobufPackage = "reside.interaction.notification.v1";

/** The well-known icon to be displayed alongside a notification action. */
export enum NotificationActionIcon {
  CONFIRMATION_ACTION_ICON_NONE = 0,
  CONFIRMATION_ACTION_ICON_CHECK = 1,
  CONFIRMATION_ACTION_ICON_CROSS = 2,
  UNRECOGNIZED = -1,
}

export function notificationActionIconFromJSON(object: any): NotificationActionIcon {
  switch (object) {
    case 0:
    case "CONFIRMATION_ACTION_ICON_NONE":
      return NotificationActionIcon.CONFIRMATION_ACTION_ICON_NONE;
    case 1:
    case "CONFIRMATION_ACTION_ICON_CHECK":
      return NotificationActionIcon.CONFIRMATION_ACTION_ICON_CHECK;
    case 2:
    case "CONFIRMATION_ACTION_ICON_CROSS":
      return NotificationActionIcon.CONFIRMATION_ACTION_ICON_CROSS;
    case -1:
    case "UNRECOGNIZED":
    default:
      return NotificationActionIcon.UNRECOGNIZED;
  }
}

export function notificationActionIconToJSON(object: NotificationActionIcon): string {
  switch (object) {
    case NotificationActionIcon.CONFIRMATION_ACTION_ICON_NONE:
      return "CONFIRMATION_ACTION_ICON_NONE";
    case NotificationActionIcon.CONFIRMATION_ACTION_ICON_CHECK:
      return "CONFIRMATION_ACTION_ICON_CHECK";
    case NotificationActionIcon.CONFIRMATION_ACTION_ICON_CROSS:
      return "CONFIRMATION_ACTION_ICON_CROSS";
    case NotificationActionIcon.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/** The action that a user can take in response to a notification, represented by a button in the UI. */
export interface NotificationAction {
  /** The unique name of the action within the notification, used to identify which action was taken by the user in the response. */
  name: string;
  /** The human-readable title of the action, displayed on the button in the UI. */
  title: string;
  /** The optional well-known icon to be displayed alongside the action in the UI, used to visually convey the purpose of the action. */
  icon: NotificationActionIcon;
}

/** The request message for sending a notification to a channel. */
export interface SendNotificationRequest {
  /**
   * The ID of the interaction context to send the notification in response to.
   * This ID is opaque and must not be parsed.
   */
  contextId: string;
  /** The channel to send the notification to. */
  channel: string;
  /**
   * The name of the partition inside the channel to send the notification to.
   * If not provided, the notification will be sent to the default partition.
   * The partition is used to scope notifications within a channel for some specific entity.
   * Partitions can be muted independently by the user, so they can be used to avoid sending notifications about some entities that the user is not interested in.
   */
  partition:
    | string
    | undefined;
  /** The human-readable title of the notification. */
  title: string;
  /** The optional notification content. */
  content:
    | string
    | undefined;
  /** The optinal list of images to be displayed in the notification. */
  images: InlineFile[];
  /**
   * The optional list of files to be attached to the notification.
   * Unlike images, these files are not actively displayed in the notification, but can be accessed by the user in some other way.
   */
  attachments: InlineFile[];
  /**
   * The optional list of actions that the user can take in response to the notification.
   * If provided with `requires_text_response` set to true, the user will have choice to either click one of the provided actions or provide a text response.
   */
  actions: NotificationAction[];
  /**
   * Whether the notification needs (or may) text input from the user in response.
   * If not true, the response to the notification will not be tracked and no operation will be created to await it.
   */
  requiresTextResponse:
    | boolean
    | undefined;
  /**
   * Whether response interaction with this notification is protected by permissions.
   * If false (default), any user can interact with notification responses.
   * If true, the interaction implementation may enforce additional authorization checks before accepting responses.
   */
  protected:
    | boolean
    | undefined;
  /**
   * Optional subject identifier to send notification on behalf of.
   * If not provided, the notification is sent on behalf of the calling replica subject.
   * Implementations may require additional permission checks to allow impersonation.
   */
  sendAsSubjectId: string | undefined;
}

/** The response message for a notification sent to a channel. */
export interface SendNotificationResponse {
  /**
   * The identifier of the created notification.
   * This ID is opaque and must not be parsed.
   */
  notificationId: string;
  /**
   * The optional operation representing the pending response to the notification.
   * If the notification has no actions and does not set `requires_text_response` to true, this will be null, as there is no response to await.
   * Once user takes an action or provides a text response, the operation will be resolved with the NotificationResponse message.
   */
  operation: Operation | undefined;
}

/** The request message for updating an existing notification. */
export interface UpdateNotificationRequest {
  /**
   * The identifier of the notification to update.
   * This ID is opaque and must not be parsed.
   */
  notificationId: string;
  /** The updated notification title. */
  title: string;
  /** The updated notification content. */
  content: string;
  /** The updated list of actions available to the user. */
  actions: NotificationAction[];
  /**
   * The updated text response requirement.
   * If not provided, the current requirement is preserved.
   */
  requiresTextResponse: boolean | undefined;
}

/** The response message for a notification update operation. */
export interface UpdateNotificationResponse {
  /**
   * The optional operation representing the pending response to the updated notification.
   * If no response tracking is required after update, this will be null.
   */
  operation: Operation | undefined;
}

/** The message representing the user's response to a notification, either by taking one of the provided actions or by providing a text response. */
export interface NotificationResponse {
  response:
    | //
    /** The name of the action taken by the user, if the user responded by taking one of the provided actions. */
    { $case: "actionName"; value: string }
    | //
    /** The text response provided by the user, if `requires_text_response` was set to true and the user responded by providing a text response instead of taking one of the provided actions. */
    { $case: "textResponse"; value: string }
    | undefined;
}

function createBaseNotificationAction(): NotificationAction {
  return { name: "", title: "", icon: 0 };
}

export const NotificationAction: MessageFns<NotificationAction> = {
  encode(message: NotificationAction, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.title !== "") {
      writer.uint32(18).string(message.title);
    }
    if (message.icon !== 0) {
      writer.uint32(24).int32(message.icon);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): NotificationAction {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseNotificationAction();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.title = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.icon = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): NotificationAction {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      title: isSet(object.title) ? globalThis.String(object.title) : "",
      icon: isSet(object.icon) ? notificationActionIconFromJSON(object.icon) : 0,
    };
  },

  toJSON(message: NotificationAction): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.title !== "") {
      obj.title = message.title;
    }
    if (message.icon !== 0) {
      obj.icon = notificationActionIconToJSON(message.icon);
    }
    return obj;
  },

  create(base?: DeepPartial<NotificationAction>): NotificationAction {
    return NotificationAction.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<NotificationAction>): NotificationAction {
    const message = createBaseNotificationAction();
    message.name = object.name ?? "";
    message.title = object.title ?? "";
    message.icon = object.icon ?? 0;
    return message;
  },
};

function createBaseSendNotificationRequest(): SendNotificationRequest {
  return {
    contextId: "",
    channel: "",
    partition: undefined,
    title: "",
    content: undefined,
    images: [],
    attachments: [],
    actions: [],
    requiresTextResponse: undefined,
    protected: undefined,
    sendAsSubjectId: undefined,
  };
}

export const SendNotificationRequest: MessageFns<SendNotificationRequest> = {
  encode(message: SendNotificationRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.contextId !== "") {
      writer.uint32(10).string(message.contextId);
    }
    if (message.channel !== "") {
      writer.uint32(18).string(message.channel);
    }
    if (message.partition !== undefined) {
      StringValue.encode({ value: message.partition! }, writer.uint32(26).fork()).join();
    }
    if (message.title !== "") {
      writer.uint32(34).string(message.title);
    }
    if (message.content !== undefined) {
      StringValue.encode({ value: message.content! }, writer.uint32(42).fork()).join();
    }
    for (const v of message.images) {
      InlineFile.encode(v!, writer.uint32(50).fork()).join();
    }
    for (const v of message.attachments) {
      InlineFile.encode(v!, writer.uint32(58).fork()).join();
    }
    for (const v of message.actions) {
      NotificationAction.encode(v!, writer.uint32(66).fork()).join();
    }
    if (message.requiresTextResponse !== undefined) {
      BoolValue.encode({ value: message.requiresTextResponse! }, writer.uint32(74).fork()).join();
    }
    if (message.protected !== undefined) {
      BoolValue.encode({ value: message.protected! }, writer.uint32(82).fork()).join();
    }
    if (message.sendAsSubjectId !== undefined) {
      StringValue.encode({ value: message.sendAsSubjectId! }, writer.uint32(90).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SendNotificationRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSendNotificationRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.contextId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.channel = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.partition = StringValue.decode(reader, reader.uint32()).value;
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.title = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.content = StringValue.decode(reader, reader.uint32()).value;
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.images.push(InlineFile.decode(reader, reader.uint32()));
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.attachments.push(InlineFile.decode(reader, reader.uint32()));
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.actions.push(NotificationAction.decode(reader, reader.uint32()));
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.requiresTextResponse = BoolValue.decode(reader, reader.uint32()).value;
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.protected = BoolValue.decode(reader, reader.uint32()).value;
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.sendAsSubjectId = StringValue.decode(reader, reader.uint32()).value;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SendNotificationRequest {
    return {
      contextId: isSet(object.contextId)
        ? globalThis.String(object.contextId)
        : isSet(object.context_id)
        ? globalThis.String(object.context_id)
        : "",
      channel: isSet(object.channel) ? globalThis.String(object.channel) : "",
      partition: isSet(object.partition) ? String(object.partition) : undefined,
      title: isSet(object.title) ? globalThis.String(object.title) : "",
      content: isSet(object.content) ? String(object.content) : undefined,
      images: globalThis.Array.isArray(object?.images) ? object.images.map((e: any) => InlineFile.fromJSON(e)) : [],
      attachments: globalThis.Array.isArray(object?.attachments)
        ? object.attachments.map((e: any) => InlineFile.fromJSON(e))
        : [],
      actions: globalThis.Array.isArray(object?.actions)
        ? object.actions.map((e: any) => NotificationAction.fromJSON(e))
        : [],
      requiresTextResponse: isSet(object.requiresTextResponse)
        ? Boolean(object.requiresTextResponse)
        : isSet(object.requires_text_response)
        ? Boolean(object.requires_text_response)
        : undefined,
      protected: isSet(object.protected) ? Boolean(object.protected) : undefined,
      sendAsSubjectId: isSet(object.sendAsSubjectId)
        ? String(object.sendAsSubjectId)
        : isSet(object.send_as_subject_id)
        ? String(object.send_as_subject_id)
        : undefined,
    };
  },

  toJSON(message: SendNotificationRequest): unknown {
    const obj: any = {};
    if (message.contextId !== "") {
      obj.contextId = message.contextId;
    }
    if (message.channel !== "") {
      obj.channel = message.channel;
    }
    if (message.partition !== undefined) {
      obj.partition = message.partition;
    }
    if (message.title !== "") {
      obj.title = message.title;
    }
    if (message.content !== undefined) {
      obj.content = message.content;
    }
    if (message.images?.length) {
      obj.images = message.images.map((e) => InlineFile.toJSON(e));
    }
    if (message.attachments?.length) {
      obj.attachments = message.attachments.map((e) => InlineFile.toJSON(e));
    }
    if (message.actions?.length) {
      obj.actions = message.actions.map((e) => NotificationAction.toJSON(e));
    }
    if (message.requiresTextResponse !== undefined) {
      obj.requiresTextResponse = message.requiresTextResponse;
    }
    if (message.protected !== undefined) {
      obj.protected = message.protected;
    }
    if (message.sendAsSubjectId !== undefined) {
      obj.sendAsSubjectId = message.sendAsSubjectId;
    }
    return obj;
  },

  create(base?: DeepPartial<SendNotificationRequest>): SendNotificationRequest {
    return SendNotificationRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SendNotificationRequest>): SendNotificationRequest {
    const message = createBaseSendNotificationRequest();
    message.contextId = object.contextId ?? "";
    message.channel = object.channel ?? "";
    message.partition = object.partition ?? undefined;
    message.title = object.title ?? "";
    message.content = object.content ?? undefined;
    message.images = object.images?.map((e) => InlineFile.fromPartial(e)) || [];
    message.attachments = object.attachments?.map((e) => InlineFile.fromPartial(e)) || [];
    message.actions = object.actions?.map((e) => NotificationAction.fromPartial(e)) || [];
    message.requiresTextResponse = object.requiresTextResponse ?? undefined;
    message.protected = object.protected ?? undefined;
    message.sendAsSubjectId = object.sendAsSubjectId ?? undefined;
    return message;
  },
};

function createBaseSendNotificationResponse(): SendNotificationResponse {
  return { notificationId: "", operation: undefined };
}

export const SendNotificationResponse: MessageFns<SendNotificationResponse> = {
  encode(message: SendNotificationResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.notificationId !== "") {
      writer.uint32(18).string(message.notificationId);
    }
    if (message.operation !== undefined) {
      Operation.encode(message.operation, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SendNotificationResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSendNotificationResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.notificationId = reader.string();
          continue;
        }
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.operation = Operation.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SendNotificationResponse {
    return {
      notificationId: isSet(object.notificationId)
        ? globalThis.String(object.notificationId)
        : isSet(object.notification_id)
        ? globalThis.String(object.notification_id)
        : "",
      operation: isSet(object.operation) ? Operation.fromJSON(object.operation) : undefined,
    };
  },

  toJSON(message: SendNotificationResponse): unknown {
    const obj: any = {};
    if (message.notificationId !== "") {
      obj.notificationId = message.notificationId;
    }
    if (message.operation !== undefined) {
      obj.operation = Operation.toJSON(message.operation);
    }
    return obj;
  },

  create(base?: DeepPartial<SendNotificationResponse>): SendNotificationResponse {
    return SendNotificationResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SendNotificationResponse>): SendNotificationResponse {
    const message = createBaseSendNotificationResponse();
    message.notificationId = object.notificationId ?? "";
    message.operation = (object.operation !== undefined && object.operation !== null)
      ? Operation.fromPartial(object.operation)
      : undefined;
    return message;
  },
};

function createBaseUpdateNotificationRequest(): UpdateNotificationRequest {
  return { notificationId: "", title: "", content: "", actions: [], requiresTextResponse: undefined };
}

export const UpdateNotificationRequest: MessageFns<UpdateNotificationRequest> = {
  encode(message: UpdateNotificationRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.notificationId !== "") {
      writer.uint32(10).string(message.notificationId);
    }
    if (message.title !== "") {
      writer.uint32(18).string(message.title);
    }
    if (message.content !== "") {
      writer.uint32(26).string(message.content);
    }
    for (const v of message.actions) {
      NotificationAction.encode(v!, writer.uint32(34).fork()).join();
    }
    if (message.requiresTextResponse !== undefined) {
      BoolValue.encode({ value: message.requiresTextResponse! }, writer.uint32(42).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UpdateNotificationRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUpdateNotificationRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.notificationId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.title = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.content = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.actions.push(NotificationAction.decode(reader, reader.uint32()));
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.requiresTextResponse = BoolValue.decode(reader, reader.uint32()).value;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UpdateNotificationRequest {
    return {
      notificationId: isSet(object.notificationId)
        ? globalThis.String(object.notificationId)
        : isSet(object.notification_id)
        ? globalThis.String(object.notification_id)
        : "",
      title: isSet(object.title) ? globalThis.String(object.title) : "",
      content: isSet(object.content) ? globalThis.String(object.content) : "",
      actions: globalThis.Array.isArray(object?.actions)
        ? object.actions.map((e: any) => NotificationAction.fromJSON(e))
        : [],
      requiresTextResponse: isSet(object.requiresTextResponse)
        ? Boolean(object.requiresTextResponse)
        : isSet(object.requires_text_response)
        ? Boolean(object.requires_text_response)
        : undefined,
    };
  },

  toJSON(message: UpdateNotificationRequest): unknown {
    const obj: any = {};
    if (message.notificationId !== "") {
      obj.notificationId = message.notificationId;
    }
    if (message.title !== "") {
      obj.title = message.title;
    }
    if (message.content !== "") {
      obj.content = message.content;
    }
    if (message.actions?.length) {
      obj.actions = message.actions.map((e) => NotificationAction.toJSON(e));
    }
    if (message.requiresTextResponse !== undefined) {
      obj.requiresTextResponse = message.requiresTextResponse;
    }
    return obj;
  },

  create(base?: DeepPartial<UpdateNotificationRequest>): UpdateNotificationRequest {
    return UpdateNotificationRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<UpdateNotificationRequest>): UpdateNotificationRequest {
    const message = createBaseUpdateNotificationRequest();
    message.notificationId = object.notificationId ?? "";
    message.title = object.title ?? "";
    message.content = object.content ?? "";
    message.actions = object.actions?.map((e) => NotificationAction.fromPartial(e)) || [];
    message.requiresTextResponse = object.requiresTextResponse ?? undefined;
    return message;
  },
};

function createBaseUpdateNotificationResponse(): UpdateNotificationResponse {
  return { operation: undefined };
}

export const UpdateNotificationResponse: MessageFns<UpdateNotificationResponse> = {
  encode(message: UpdateNotificationResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.operation !== undefined) {
      Operation.encode(message.operation, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UpdateNotificationResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUpdateNotificationResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.operation = Operation.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UpdateNotificationResponse {
    return { operation: isSet(object.operation) ? Operation.fromJSON(object.operation) : undefined };
  },

  toJSON(message: UpdateNotificationResponse): unknown {
    const obj: any = {};
    if (message.operation !== undefined) {
      obj.operation = Operation.toJSON(message.operation);
    }
    return obj;
  },

  create(base?: DeepPartial<UpdateNotificationResponse>): UpdateNotificationResponse {
    return UpdateNotificationResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<UpdateNotificationResponse>): UpdateNotificationResponse {
    const message = createBaseUpdateNotificationResponse();
    message.operation = (object.operation !== undefined && object.operation !== null)
      ? Operation.fromPartial(object.operation)
      : undefined;
    return message;
  },
};

function createBaseNotificationResponse(): NotificationResponse {
  return { response: undefined };
}

export const NotificationResponse: MessageFns<NotificationResponse> = {
  encode(message: NotificationResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    switch (message.response?.$case) {
      case "actionName":
        writer.uint32(10).string(message.response.value);
        break;
      case "textResponse":
        writer.uint32(18).string(message.response.value);
        break;
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): NotificationResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseNotificationResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.response = { $case: "actionName", value: reader.string() };
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.response = { $case: "textResponse", value: reader.string() };
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): NotificationResponse {
    return {
      response: isSet(object.actionName)
        ? { $case: "actionName", value: globalThis.String(object.actionName) }
        : isSet(object.action_name)
        ? { $case: "actionName", value: globalThis.String(object.action_name) }
        : isSet(object.textResponse)
        ? { $case: "textResponse", value: globalThis.String(object.textResponse) }
        : isSet(object.text_response)
        ? { $case: "textResponse", value: globalThis.String(object.text_response) }
        : undefined,
    };
  },

  toJSON(message: NotificationResponse): unknown {
    const obj: any = {};
    if (message.response?.$case === "actionName") {
      obj.actionName = message.response.value;
    } else if (message.response?.$case === "textResponse") {
      obj.textResponse = message.response.value;
    }
    return obj;
  },

  create(base?: DeepPartial<NotificationResponse>): NotificationResponse {
    return NotificationResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<NotificationResponse>): NotificationResponse {
    const message = createBaseNotificationResponse();
    switch (object.response?.$case) {
      case "actionName": {
        if (object.response?.value !== undefined && object.response?.value !== null) {
          message.response = { $case: "actionName", value: object.response.value };
        }
        break;
      }
      case "textResponse": {
        if (object.response?.value !== undefined && object.response?.value !== null) {
          message.response = { $case: "textResponse", value: object.response.value };
        }
        break;
      }
    }
    return message;
  },
};

/** The notification service, which allows sending notifications to users and receiving their responses. */
export type NotificationServiceDefinition = typeof NotificationServiceDefinition;
export const NotificationServiceDefinition = {
  name: "NotificationService",
  fullName: "reside.interaction.notification.v1.NotificationService",
  methods: {
    /** Sends a notification to a channel. */
    sendNotification: {
      name: "SendNotification",
      requestType: SendNotificationRequest as typeof SendNotificationRequest,
      requestStream: false,
      responseType: SendNotificationResponse as typeof SendNotificationResponse,
      responseStream: false,
      options: {},
    },
    /** Updates title and content of an existing notification. */
    updateNotification: {
      name: "UpdateNotification",
      requestType: UpdateNotificationRequest as typeof UpdateNotificationRequest,
      requestStream: false,
      responseType: UpdateNotificationResponse as typeof UpdateNotificationResponse,
      responseStream: false,
      options: {},
    },
  },
} as const;

export interface NotificationServiceImplementation<CallContextExt = {}> {
  /** Sends a notification to a channel. */
  sendNotification(
    request: SendNotificationRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<SendNotificationResponse>>;
  /** Updates title and content of an existing notification. */
  updateNotification(
    request: UpdateNotificationRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<UpdateNotificationResponse>>;
}

export interface NotificationServiceClient<CallOptionsExt = {}> {
  /** Sends a notification to a channel. */
  sendNotification(
    request: DeepPartial<SendNotificationRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<SendNotificationResponse>;
  /** Updates title and content of an existing notification. */
  updateNotification(
    request: DeepPartial<UpdateNotificationRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<UpdateNotificationResponse>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string; value: unknown } ? { $case: T["$case"]; value?: DeepPartial<T["value"]> }
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
