// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.5
//   protoc               v3.19.1
// source: interaction/definition.v1.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import type { CallContext, CallOptions } from "nice-grpc-common";
import { BoolValue, StringValue } from "../google/protobuf/wrappers";

export const protobufPackage = "reside.interaction.definition.v1";

/** The CommandParameterType enum defines the possible types of parameters that can be passed to a command when executing it. */
export enum CommandParameterType {
  /** STRING - The command parameter is a string value. */
  STRING = 0,
  /** INTEGER - The command parameter is an integer value. */
  INTEGER = 1,
  /** BOOLEAN - The command parameter is a boolean value. */
  BOOLEAN = 2,
  UNRECOGNIZED = -1,
}

export function commandParameterTypeFromJSON(object: any): CommandParameterType {
  switch (object) {
    case 0:
    case "COMMAND_PARAMETER_TYPE_STRING":
      return CommandParameterType.STRING;
    case 1:
    case "COMMAND_PARAMETER_TYPE_INTEGER":
      return CommandParameterType.INTEGER;
    case 2:
    case "COMMAND_PARAMETER_TYPE_BOOLEAN":
      return CommandParameterType.BOOLEAN;
    case -1:
    case "UNRECOGNIZED":
    default:
      return CommandParameterType.UNRECOGNIZED;
  }
}

export function commandParameterTypeToJSON(object: CommandParameterType): string {
  switch (object) {
    case CommandParameterType.STRING:
      return "COMMAND_PARAMETER_TYPE_STRING";
    case CommandParameterType.INTEGER:
      return "COMMAND_PARAMETER_TYPE_INTEGER";
    case CommandParameterType.BOOLEAN:
      return "COMMAND_PARAMETER_TYPE_BOOLEAN";
    case CommandParameterType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/**
 * The channel that scopes notifications.
 * Each channel can be muted.
 */
export interface NotificationChannel {
  /** The unique identifier of the channel. */
  id: number;
  /** The unique name of the channel. */
  name: string;
  /** The required human-readable name of the channel. */
  title: string;
  /** The optional description of the channel. */
  description: string | undefined;
}

/** The input message for creating or updating a notification channel. */
export interface ChannelInput {
  /** The unique name of the channel. */
  name: string;
  /** The required human-readable name of the channel. */
  title: string;
  /** The optional description of the channel. */
  description: string | undefined;
}

/** The request to create or update multiple notification channels in the system at once. */
export interface PutChannelsRequest {
  /** The list of channels to create or update. Each channel in the list must have a unique name. */
  channels: ChannelInput[];
}

/** The response message for PutChannelsRequest, containing the list of created or updated channels. */
export interface PutChannelsResponse {
  /** The list of channels that were created or updated as a result of the PutChannelsRequest. */
  channels: NotificationChannel[];
}

/** The command provides a structured representation of an executable action that can be triggered within the system. */
export interface Command {
  /** The unique numeric identifier of the command. */
  id: number;
  /** The unique name of the command. */
  name: string;
  /** The required human-readable name of the command. */
  title: string;
  /** The optional description of the command. */
  description:
    | string
    | undefined;
  /** The list of parameters that can be passed to the command when executing it. */
  parameters: CommandParameter[];
  /**
   * Whether invoking this command requires explicit permission checks.
   * If false (default), any user may invoke this command.
   */
  protected:
    | boolean
    | undefined;
  /** The gRPC endpoint of the command handler service for this command. */
  callbackEndpoint: string;
}

/** The CommandParameter represents a single parameter that can be passed to a command when executing it. */
export interface CommandParameter {
  /** The unique name of the parameter within the command, used to identify the parameter when executing the command. */
  name: string;
  /** The human-readable title of the parameter, used in the UI to label input fields for the parameter. */
  title: string;
  /** The optional description of the parameter. */
  description:
    | string
    | undefined;
  /** The type of the parameter, which determines the expected format of the parameter value when executing the command. */
  type: CommandParameterType;
  /** Whether the parameter is required when invoking the command. */
  required: boolean;
  /** Whether this parameter captures the rest of the command arguments as a single value. */
  rest: boolean;
}

/** The input to create or update a command in the system. */
export interface CommandInput {
  /** The unique name of the command. */
  name: string;
  /** The required human-readable name of the command. */
  title: string;
  /** The optional description of the command. */
  description:
    | string
    | undefined;
  /** The list of parameters that can be passed to the command when executing it. */
  parameters: CommandParameter[];
  /**
   * Whether invoking this command requires explicit permission checks.
   * If false (default), any user may invoke this command.
   */
  protected:
    | boolean
    | undefined;
  /** The gRPC endpoint of the command handler service for this command. */
  callbackEndpoint: string;
}

/** The request to create or update multiple commands in the system at once. */
export interface PutCommandsRequest {
  /** The list of commands to create or update. Each command in the list must have a unique name. */
  commands: CommandInput[];
}

/** The response message for PutCommandsRequest, containing the list of created or updated commands. */
export interface PutCommandsResponse {
  /** The list of commands that were created or updated as a result of the PutCommandsRequest. */
  commands: Command[];
}

function createBaseNotificationChannel(): NotificationChannel {
  return { id: 0, name: "", title: "", description: undefined };
}

export const NotificationChannel: MessageFns<NotificationChannel> = {
  encode(message: NotificationChannel, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== 0) {
      writer.uint32(8).int32(message.id);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    if (message.title !== "") {
      writer.uint32(26).string(message.title);
    }
    if (message.description !== undefined) {
      StringValue.encode({ value: message.description! }, writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): NotificationChannel {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseNotificationChannel();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.id = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.title = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.description = StringValue.decode(reader, reader.uint32()).value;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): NotificationChannel {
    return {
      id: isSet(object.id) ? globalThis.Number(object.id) : 0,
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      title: isSet(object.title) ? globalThis.String(object.title) : "",
      description: isSet(object.description) ? String(object.description) : undefined,
    };
  },

  toJSON(message: NotificationChannel): unknown {
    const obj: any = {};
    if (message.id !== 0) {
      obj.id = Math.round(message.id);
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.title !== "") {
      obj.title = message.title;
    }
    if (message.description !== undefined) {
      obj.description = message.description;
    }
    return obj;
  },

  create(base?: DeepPartial<NotificationChannel>): NotificationChannel {
    return NotificationChannel.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<NotificationChannel>): NotificationChannel {
    const message = createBaseNotificationChannel();
    message.id = object.id ?? 0;
    message.name = object.name ?? "";
    message.title = object.title ?? "";
    message.description = object.description ?? undefined;
    return message;
  },
};

function createBaseChannelInput(): ChannelInput {
  return { name: "", title: "", description: undefined };
}

export const ChannelInput: MessageFns<ChannelInput> = {
  encode(message: ChannelInput, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.title !== "") {
      writer.uint32(18).string(message.title);
    }
    if (message.description !== undefined) {
      StringValue.encode({ value: message.description! }, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ChannelInput {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseChannelInput();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.title = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.description = StringValue.decode(reader, reader.uint32()).value;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ChannelInput {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      title: isSet(object.title) ? globalThis.String(object.title) : "",
      description: isSet(object.description) ? String(object.description) : undefined,
    };
  },

  toJSON(message: ChannelInput): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.title !== "") {
      obj.title = message.title;
    }
    if (message.description !== undefined) {
      obj.description = message.description;
    }
    return obj;
  },

  create(base?: DeepPartial<ChannelInput>): ChannelInput {
    return ChannelInput.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ChannelInput>): ChannelInput {
    const message = createBaseChannelInput();
    message.name = object.name ?? "";
    message.title = object.title ?? "";
    message.description = object.description ?? undefined;
    return message;
  },
};

function createBasePutChannelsRequest(): PutChannelsRequest {
  return { channels: [] };
}

export const PutChannelsRequest: MessageFns<PutChannelsRequest> = {
  encode(message: PutChannelsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.channels) {
      ChannelInput.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PutChannelsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePutChannelsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.channels.push(ChannelInput.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PutChannelsRequest {
    return {
      channels: globalThis.Array.isArray(object?.channels)
        ? object.channels.map((e: any) => ChannelInput.fromJSON(e))
        : [],
    };
  },

  toJSON(message: PutChannelsRequest): unknown {
    const obj: any = {};
    if (message.channels?.length) {
      obj.channels = message.channels.map((e) => ChannelInput.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<PutChannelsRequest>): PutChannelsRequest {
    return PutChannelsRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PutChannelsRequest>): PutChannelsRequest {
    const message = createBasePutChannelsRequest();
    message.channels = object.channels?.map((e) => ChannelInput.fromPartial(e)) || [];
    return message;
  },
};

function createBasePutChannelsResponse(): PutChannelsResponse {
  return { channels: [] };
}

export const PutChannelsResponse: MessageFns<PutChannelsResponse> = {
  encode(message: PutChannelsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.channels) {
      NotificationChannel.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PutChannelsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePutChannelsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.channels.push(NotificationChannel.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PutChannelsResponse {
    return {
      channels: globalThis.Array.isArray(object?.channels)
        ? object.channels.map((e: any) => NotificationChannel.fromJSON(e))
        : [],
    };
  },

  toJSON(message: PutChannelsResponse): unknown {
    const obj: any = {};
    if (message.channels?.length) {
      obj.channels = message.channels.map((e) => NotificationChannel.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<PutChannelsResponse>): PutChannelsResponse {
    return PutChannelsResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PutChannelsResponse>): PutChannelsResponse {
    const message = createBasePutChannelsResponse();
    message.channels = object.channels?.map((e) => NotificationChannel.fromPartial(e)) || [];
    return message;
  },
};

function createBaseCommand(): Command {
  return {
    id: 0,
    name: "",
    title: "",
    description: undefined,
    parameters: [],
    protected: undefined,
    callbackEndpoint: "",
  };
}

export const Command: MessageFns<Command> = {
  encode(message: Command, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== 0) {
      writer.uint32(8).int32(message.id);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    if (message.title !== "") {
      writer.uint32(26).string(message.title);
    }
    if (message.description !== undefined) {
      StringValue.encode({ value: message.description! }, writer.uint32(34).fork()).join();
    }
    for (const v of message.parameters) {
      CommandParameter.encode(v!, writer.uint32(42).fork()).join();
    }
    if (message.protected !== undefined) {
      BoolValue.encode({ value: message.protected! }, writer.uint32(50).fork()).join();
    }
    if (message.callbackEndpoint !== "") {
      writer.uint32(58).string(message.callbackEndpoint);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Command {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCommand();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.id = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.title = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.description = StringValue.decode(reader, reader.uint32()).value;
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.parameters.push(CommandParameter.decode(reader, reader.uint32()));
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.protected = BoolValue.decode(reader, reader.uint32()).value;
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.callbackEndpoint = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Command {
    return {
      id: isSet(object.id) ? globalThis.Number(object.id) : 0,
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      title: isSet(object.title) ? globalThis.String(object.title) : "",
      description: isSet(object.description) ? String(object.description) : undefined,
      parameters: globalThis.Array.isArray(object?.parameters)
        ? object.parameters.map((e: any) => CommandParameter.fromJSON(e))
        : [],
      protected: isSet(object.protected) ? Boolean(object.protected) : undefined,
      callbackEndpoint: isSet(object.callbackEndpoint)
        ? globalThis.String(object.callbackEndpoint)
        : isSet(object.callback_endpoint)
        ? globalThis.String(object.callback_endpoint)
        : "",
    };
  },

  toJSON(message: Command): unknown {
    const obj: any = {};
    if (message.id !== 0) {
      obj.id = Math.round(message.id);
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.title !== "") {
      obj.title = message.title;
    }
    if (message.description !== undefined) {
      obj.description = message.description;
    }
    if (message.parameters?.length) {
      obj.parameters = message.parameters.map((e) => CommandParameter.toJSON(e));
    }
    if (message.protected !== undefined) {
      obj.protected = message.protected;
    }
    if (message.callbackEndpoint !== "") {
      obj.callbackEndpoint = message.callbackEndpoint;
    }
    return obj;
  },

  create(base?: DeepPartial<Command>): Command {
    return Command.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Command>): Command {
    const message = createBaseCommand();
    message.id = object.id ?? 0;
    message.name = object.name ?? "";
    message.title = object.title ?? "";
    message.description = object.description ?? undefined;
    message.parameters = object.parameters?.map((e) => CommandParameter.fromPartial(e)) || [];
    message.protected = object.protected ?? undefined;
    message.callbackEndpoint = object.callbackEndpoint ?? "";
    return message;
  },
};

function createBaseCommandParameter(): CommandParameter {
  return { name: "", title: "", description: undefined, type: 0, required: false, rest: false };
}

export const CommandParameter: MessageFns<CommandParameter> = {
  encode(message: CommandParameter, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.title !== "") {
      writer.uint32(18).string(message.title);
    }
    if (message.description !== undefined) {
      StringValue.encode({ value: message.description! }, writer.uint32(26).fork()).join();
    }
    if (message.type !== 0) {
      writer.uint32(32).int32(message.type);
    }
    if (message.required !== false) {
      writer.uint32(40).bool(message.required);
    }
    if (message.rest !== false) {
      writer.uint32(48).bool(message.rest);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CommandParameter {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCommandParameter();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.title = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.description = StringValue.decode(reader, reader.uint32()).value;
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.type = reader.int32() as any;
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.required = reader.bool();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.rest = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CommandParameter {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      title: isSet(object.title) ? globalThis.String(object.title) : "",
      description: isSet(object.description) ? String(object.description) : undefined,
      type: isSet(object.type) ? commandParameterTypeFromJSON(object.type) : 0,
      required: isSet(object.required) ? globalThis.Boolean(object.required) : false,
      rest: isSet(object.rest) ? globalThis.Boolean(object.rest) : false,
    };
  },

  toJSON(message: CommandParameter): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.title !== "") {
      obj.title = message.title;
    }
    if (message.description !== undefined) {
      obj.description = message.description;
    }
    if (message.type !== 0) {
      obj.type = commandParameterTypeToJSON(message.type);
    }
    if (message.required !== false) {
      obj.required = message.required;
    }
    if (message.rest !== false) {
      obj.rest = message.rest;
    }
    return obj;
  },

  create(base?: DeepPartial<CommandParameter>): CommandParameter {
    return CommandParameter.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<CommandParameter>): CommandParameter {
    const message = createBaseCommandParameter();
    message.name = object.name ?? "";
    message.title = object.title ?? "";
    message.description = object.description ?? undefined;
    message.type = object.type ?? 0;
    message.required = object.required ?? false;
    message.rest = object.rest ?? false;
    return message;
  },
};

function createBaseCommandInput(): CommandInput {
  return { name: "", title: "", description: undefined, parameters: [], protected: undefined, callbackEndpoint: "" };
}

export const CommandInput: MessageFns<CommandInput> = {
  encode(message: CommandInput, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.title !== "") {
      writer.uint32(18).string(message.title);
    }
    if (message.description !== undefined) {
      StringValue.encode({ value: message.description! }, writer.uint32(26).fork()).join();
    }
    for (const v of message.parameters) {
      CommandParameter.encode(v!, writer.uint32(34).fork()).join();
    }
    if (message.protected !== undefined) {
      BoolValue.encode({ value: message.protected! }, writer.uint32(42).fork()).join();
    }
    if (message.callbackEndpoint !== "") {
      writer.uint32(50).string(message.callbackEndpoint);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CommandInput {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCommandInput();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.title = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.description = StringValue.decode(reader, reader.uint32()).value;
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.parameters.push(CommandParameter.decode(reader, reader.uint32()));
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.protected = BoolValue.decode(reader, reader.uint32()).value;
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.callbackEndpoint = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CommandInput {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      title: isSet(object.title) ? globalThis.String(object.title) : "",
      description: isSet(object.description) ? String(object.description) : undefined,
      parameters: globalThis.Array.isArray(object?.parameters)
        ? object.parameters.map((e: any) => CommandParameter.fromJSON(e))
        : [],
      protected: isSet(object.protected) ? Boolean(object.protected) : undefined,
      callbackEndpoint: isSet(object.callbackEndpoint)
        ? globalThis.String(object.callbackEndpoint)
        : isSet(object.callback_endpoint)
        ? globalThis.String(object.callback_endpoint)
        : "",
    };
  },

  toJSON(message: CommandInput): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.title !== "") {
      obj.title = message.title;
    }
    if (message.description !== undefined) {
      obj.description = message.description;
    }
    if (message.parameters?.length) {
      obj.parameters = message.parameters.map((e) => CommandParameter.toJSON(e));
    }
    if (message.protected !== undefined) {
      obj.protected = message.protected;
    }
    if (message.callbackEndpoint !== "") {
      obj.callbackEndpoint = message.callbackEndpoint;
    }
    return obj;
  },

  create(base?: DeepPartial<CommandInput>): CommandInput {
    return CommandInput.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<CommandInput>): CommandInput {
    const message = createBaseCommandInput();
    message.name = object.name ?? "";
    message.title = object.title ?? "";
    message.description = object.description ?? undefined;
    message.parameters = object.parameters?.map((e) => CommandParameter.fromPartial(e)) || [];
    message.protected = object.protected ?? undefined;
    message.callbackEndpoint = object.callbackEndpoint ?? "";
    return message;
  },
};

function createBasePutCommandsRequest(): PutCommandsRequest {
  return { commands: [] };
}

export const PutCommandsRequest: MessageFns<PutCommandsRequest> = {
  encode(message: PutCommandsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.commands) {
      CommandInput.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PutCommandsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePutCommandsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.commands.push(CommandInput.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PutCommandsRequest {
    return {
      commands: globalThis.Array.isArray(object?.commands)
        ? object.commands.map((e: any) => CommandInput.fromJSON(e))
        : [],
    };
  },

  toJSON(message: PutCommandsRequest): unknown {
    const obj: any = {};
    if (message.commands?.length) {
      obj.commands = message.commands.map((e) => CommandInput.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<PutCommandsRequest>): PutCommandsRequest {
    return PutCommandsRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PutCommandsRequest>): PutCommandsRequest {
    const message = createBasePutCommandsRequest();
    message.commands = object.commands?.map((e) => CommandInput.fromPartial(e)) || [];
    return message;
  },
};

function createBasePutCommandsResponse(): PutCommandsResponse {
  return { commands: [] };
}

export const PutCommandsResponse: MessageFns<PutCommandsResponse> = {
  encode(message: PutCommandsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.commands) {
      Command.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PutCommandsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePutCommandsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.commands.push(Command.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PutCommandsResponse {
    return {
      commands: globalThis.Array.isArray(object?.commands) ? object.commands.map((e: any) => Command.fromJSON(e)) : [],
    };
  },

  toJSON(message: PutCommandsResponse): unknown {
    const obj: any = {};
    if (message.commands?.length) {
      obj.commands = message.commands.map((e) => Command.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<PutCommandsResponse>): PutCommandsResponse {
    return PutCommandsResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PutCommandsResponse>): PutCommandsResponse {
    const message = createBasePutCommandsResponse();
    message.commands = object.commands?.map((e) => Command.fromPartial(e)) || [];
    return message;
  },
};

/** The definition service for notification-related operations, such as creating notification channels. */
export type DefinitionServiceDefinition = typeof DefinitionServiceDefinition;
export const DefinitionServiceDefinition = {
  name: "DefinitionService",
  fullName: "reside.interaction.definition.v1.DefinitionService",
  methods: {
    /** The request to create or update a notification channel in the system. */
    putChannels: {
      name: "PutChannels",
      requestType: PutChannelsRequest as typeof PutChannelsRequest,
      requestStream: false,
      responseType: PutChannelsResponse as typeof PutChannelsResponse,
      responseStream: false,
      options: {},
    },
    /** The request to create or update a command in the system. */
    putCommands: {
      name: "PutCommands",
      requestType: PutCommandsRequest as typeof PutCommandsRequest,
      requestStream: false,
      responseType: PutCommandsResponse as typeof PutCommandsResponse,
      responseStream: false,
      options: {},
    },
  },
} as const;

export interface DefinitionServiceImplementation<CallContextExt = {}> {
  /** The request to create or update a notification channel in the system. */
  putChannels(
    request: PutChannelsRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<PutChannelsResponse>>;
  /** The request to create or update a command in the system. */
  putCommands(
    request: PutCommandsRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<PutCommandsResponse>>;
}

export interface DefinitionServiceClient<CallOptionsExt = {}> {
  /** The request to create or update a notification channel in the system. */
  putChannels(
    request: DeepPartial<PutChannelsRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<PutChannelsResponse>;
  /** The request to create or update a command in the system. */
  putCommands(
    request: DeepPartial<PutCommandsRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<PutCommandsResponse>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string; value: unknown } ? { $case: T["$case"]; value?: DeepPartial<T["value"]> }
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
